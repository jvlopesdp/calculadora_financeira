## Codebase Patterns
- Project root is `/workspace` (Vite project lives at the repo root, not in a subdir).
- Use Bun as the task runner: `bun install`, `bun run typecheck`, `bun run lint`, `bun run test`, `bun run build`. Scripts are defined in `package.json`.
- Tailwind v4 + `@tailwindcss/vite` plugin (no `tailwind.config.js` — theme tokens come from `src/index.css`, copied from `/index.css` tweakcn export).
- Path alias `@/` -> `./src/` is set in both `vite.config.ts` and `tsconfig.app.json` — use `@/lib/...`, `@/core/finance/...`, etc.
- `vite.config.ts` imports `defineConfig` from `vitest/config` (single config file for both Vite and Vitest — avoids dual-vite type-conflict).
- shadcn/ui is initialized via `components.json` (style: new-york, baseColor: neutral, cssVariables: true). Add components with `bunx shadcn@latest add <name>`. DiceUI uses the same registry mechanism: `bunx shadcn@latest add "https://diceui.com/r/<component>.json"`.
- ESLint config (`eslint.config.js`, flat config) enforces `@typescript-eslint/no-explicit-any: error` and ignores `src/components/ui/**` (shadcn-generated code).
- ESLint also enforces `no-irregular-whitespace`. If you need to match U+00A0 / U+202F (NBSP / narrow NBSP from `Intl.NumberFormat`), build the regex via `new RegExp("[\\u00A0\\u202F]", "g")` instead of writing the literal NBSP inside a `/.../` regex literal.
- Tests live next to source as `*.test.ts(x)` or in `src/tests/`. Global `expect`/`describe` available (vitest globals + `@testing-library/jest-dom/vitest` matchers).
- Vitest uses jsdom environment; setup file at `src/tests/setup.ts`.
- CI runs on push/PR to `main`: install -> typecheck -> lint -> test -> build (`.github/workflows/ci.yml`).
- pt-BR formatters live in `src/lib/formatters/`: `currency.ts` (`formatBRL`), `percentage.ts` (`formatPercentage` / `parsePercentage`), `number.ts` (`formatInteger` / `formatMonths`). Reuse these everywhere — never re-roll a `toLocaleString` call inline.
- `Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" })` returns the symbol joined to the number with U+00A0 (and sometimes U+202F on newer ICU). The formatters strip those to a plain ASCII space so tests, snapshots, and Excel export are deterministic — keep that normalization step if you fork them.
- `formatPercentage` expects the value in **percent units** (e.g. `1.25` → `"1,25%"`), not a fraction. Internally it divides by 100 before passing to the percent-style Intl formatter. `parsePercentage` is the inverse and tolerates an optional `%`, whitespace, and pt-BR thousand/decimal separators.
- Decimal.js global precision/rounding is configured **once** in `src/core/finance/financial-types.ts` (`Decimal.set({ precision: 30, rounding: ROUND_HALF_EVEN })`). Any new finance module must import from `./financial-types` (or transitively, e.g. via `price-calculator`) so the side-effect runs before arithmetic.
- Money rounding helper: `roundMoney(value: Decimal)` in `financial-types.ts` rounds to 2dp using `ROUND_HALF_EVEN`. Use it for every monetary cell that gets recorded into a `ScheduleRow` — never call `.toDecimalPlaces` directly inside engine code so the rounding mode stays consistent.
- `Decimal#toString()` strips trailing zeros (e.g. `new Decimal("10100.00").toString() === "10100"`). For 2dp display assertions use `toFixed(2)` or compare with `.equals()`. Prefer `.equals(n)` in tests over string equality.
- Schedule rounding convention: round the base PRICE installment **once** at start, round each row's interest to 2dp from the running balance, derive amortization as `installment - interest`, and let the **final** row's amortization equal the remaining balance (final installment = balance + final-interest, may differ from base installment). This guarantees `last.balance === 0` exactly and `sum(amortization) === principal` exactly.
- Vitest matcher names follow Jest conventions: `toBeLessThanOrEqual` / `toBeGreaterThanOrEqual` (no trailing "To"). Decimal.js uses `.lessThanOrEqualTo` / `.greaterThanOrEqualTo` — different method, different name. If you mistype the matcher, Vitest throws "Invalid Chai property" with a suggestion.
- Prepayment schedules use `PrepaymentScheduleRow extends ScheduleRow` with two extra fields: `baseInstallment` (the row's "would-be" base for the strategy) and `extraPayment` (extra applied that month). Invariant: `baseInstallment + extraPayment === installment` and `interest + amortization === installment`. On partial final rows, cap recorded extra at the user-supplied cap via the `splitFinalPayment` helper so the zero-extra case doesn't leak rounding residuals into `extraPayment`.
- Annual-to-monthly rate conversion lives in `rent-vs-buy.ts` as `annualToMonthlyRate(annual)` = `(1 + annual)^(1/12) − 1` (Decimal.js `pow` with a fractional exponent). Reuse this helper for any future feature that needs to convert an annual return/appreciation/inflation rate to a per-month compounding rate so the precision setup stays consistent.
- Symmetric rent-vs-buy model: each month the lower-spending side invests `|buyOutflow − rentOutflow|` (no borrowing — the higher-spender contributes nothing). Both parties' invested capital grows by the monthly investment rate **before** the new contribution is added. Property value compounds monthly from the annual appreciation rate. Rent steps up at month 12, 24, 36, ... by `(1 + annualRentAdjustment)`. Net worth at month m = `propertyValue − outstandingBalance + investedCapital` (buy) and `investedCapital` (rent). Negative net worth is allowed.
- Rent-vs-buy timelines include month 0 as the starting state, so timeline length is `horizonMonths + 1` (m=0..horizon). At m=0 both sides start tied at `downPayment` net worth (buyer paid it as down payment, renter invested it). Break-even is the first m ≥ 1 where `buy.netWorth >= rent.netWorth`; if buy never catches up, `breakEvenMonth` is `null`. Tie tolerance for `bestScenario` is R$ 0.01 (matches money rounding).
- Zod form schemas live in `src/features/simulator/schemas/`: `financing.ts`, `prepayment.ts`, `rent-vs-buy.ts`. They validate the **form layer** (plain numbers), not the engine layer (Decimal). The form/hook layer converts numbers to Decimal before calling the engine. Reuse these via `zodResolver` in React Hook Form. All error messages are pt-BR.
- Zod 3.25 still supports the v3 classic API: `z.number({ required_error: "...", invalid_type_error: "..." })`. Use this pattern for every form field so missing values, wrong types, and out-of-range numbers each get a distinct pt-BR message. The default `z.number()` message ("Required" / "Expected number, received string") is in English — always override.
- Cross-field rules (e.g. `downPayment < propertyValue`) go in `.refine(...)` with `path: ["fieldName"]` so the error surfaces below the right input. Test by asserting `result.error.issues.find((i) => i.path.join(".") === "fieldName")?.message`.
- Tests using `safeParse` should grab errors via a small helper `firstErrorFor(result, path)` instead of relying on `error.flatten().fieldErrors[path][0]` — the latter loses cross-field refines that target a single field.
- When testing "missing value" via destructure-and-omit, ESLint's no-unused-vars rule complains about the destructured placeholder. Name the placeholder `_omit` and add `void _omit;` so the destructure has a referenced variable. Don't try `_` (collides with other ignored vars) and don't disable the rule per-file.
- React context + hook + provider: split into two files. The `createContext` call and the consumer hook (e.g. `useTheme`) live in a `.ts` file (no JSX, exports only constants/types/hook); the `<Provider>` component lives in a `.tsx` file that imports from the context file. This satisfies eslint-plugin-react-refresh's `only-export-components` rule, which would otherwise warn when a `.tsx` file exports both a component and a hook/constant. Same pattern shadcn uses for its theme provider.
- shadcn CLI (`bunx shadcn@latest add <name>`) requires network access to https://ui.shadcn.com; the sandbox has none. When you need a shadcn component, hand-roll it in `src/components/ui/<name>.tsx` using the new-york-style source (the file is ESLint-ignored already, so the typical shadcn idioms — `React.forwardRef`, `cva` variants, `cn(...)` from `@/lib/utils`, `Slot` from `@radix-ui/react-slot`, `displayName` — pass without changes).
- Theme-class convention: this project's tweakcn `index.css` uses `.dark` as the toggling class (via `@custom-variant dark (&:is(.dark *))`), applied to `document.documentElement` (`<html>`). The provider must add/remove the literal class name `dark` on `<html>` (not `<body>`, not a custom name) for the existing CSS-variable overrides in `.dark { ... }` to take effect.
- jsdom + matchMedia: jsdom doesn't ship a `window.matchMedia` implementation. In tests, stub it via `Object.defineProperty(window, "matchMedia", { configurable: true, writable: true, value: ... })` returning a fake `MediaQueryList` with `addEventListener`/`removeEventListener` that track listeners — then trigger `change` events manually to test the OS-preference subscription. `vi.stubGlobal` alone isn't enough because the `window` property is non-writable by default.
- App shell lives at `src/app/App.tsx` (not `src/App.tsx`). The entry import in `src/main.tsx` is `import App from "@/app/App"`. Keep this layout — future page-level wiring (`useSimulation`, context providers, etc.) goes under `src/app/`.
- shadcn primitives we hand-roll go in `src/components/ui/`. So far: `button.tsx`, `card.tsx`. ESLint already ignores that folder so the typical `React.forwardRef` + `cn(...)` + `displayName` idioms pass without changes. Reuse `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent` across the simulator cards — don't redefine the visual shell per feature.
- App-level layout convention: outer `<main class="mx-auto max-w-screen-xl ...">` + inner `<div class="flex flex-col gap-6 md:grid md:grid-cols-12 md:gap-6">`. Each section card sets its own `md:col-span-*`. Below the `md` (768px) breakpoint the cards stack vertically (`flex-col`); above it they fall into a 12-column grid. Header is `<header class="sticky top-0 z-40 ... shadow-sm">` so it pins on scroll.
- Reusable finance inputs live in `src/components/finance/`: `currency-input.tsx` (BRL) and `percentage-input.tsx`. API: `value: number | null` + `onChange: (v: number | null) => void` + optional `decimals` (percent). Internal display state; raw editable on focus, formatted on blur. `onChange` fires on every keystroke (parsed) so RHF validation can be `mode: "onChange"`. Reuse these via RHF `Controller` — never re-roll inline `<input>` masking.
- pt-BR currency parser: `parseBRL(input: string): number | null` lives in `src/lib/formatters/currency.ts` alongside `formatBRL` (mirrors `parsePercentage`/`formatPercentage`). Tolerates `R$`, NBSP/whitespace, pt-BR thousand dots, and decimal commas; returns `null` for blanks/non-numeric input.
- Page-level state context: `src/features/simulator/hooks/simulation-context.ts` (context + `useSimulation()` hook, .ts no JSX) + `simulation-provider.tsx` (component). Same two-file split shadcn/our theme provider uses to satisfy `react-refresh/only-export-components`. `SimulationProvider` wraps `<App />` in `src/main.tsx` and in test renders.
- Hand-rolled shadcn primitives we have so far: `button.tsx`, `card.tsx`, `input.tsx`, `label.tsx`, `select.tsx`. The Select primitive is a styled native `<select>` (no `@radix-ui/react-select` dep) — keeps the bundle slim and works fine with RHF `register`. Reuse these everywhere; do not redefine per-feature.
- RHF + zodResolver + nullable defaults: `useForm<FormShape>({ resolver: zodResolver(schema), mode: "onChange", defaultValues })` where `FormShape` has nullable fields (`number | null`). Do NOT pass the 3-generic form (`useForm<TFieldValues, TContext, TTransformedValues>`) — it conflicts with zodResolver's inferred output type when nullable fields refine to non-null. Inside `handleSubmit`, cast values to the schema's output type if downstream needs the strict shape.
- Zod `.refine()` cross-field rules (e.g. `downPayment < propertyValue`) only fire when **every** individual field validation passes. In a UI test that wants to surface the refine message, fill in **all** required fields with valid values first, then introduce the cross-field invariant violation in the field of interest. Otherwise the refine never runs and the message never appears.
- Derived read-only fields use `<output>` (not `<input readOnly>`) so screen readers announce updates via `aria-live="polite"` and the `getByLabelText` query still resolves (label `htmlFor` works on `<output>` too). Style with the same `border-input bg-muted ...` classes as inputs for visual consistency, plus `.font-tabular` for monetary values.
- Hand-rolled `Tabs` primitive lives at `src/components/ui/tabs.tsx`: `Tabs` (controlled `value` or uncontrolled via `defaultValue`), `TabsList` (`role="tablist"`), `TabsTrigger` (`role="tab"` button with `aria-selected`/`aria-controls`/`tabIndex`), `TabsContent` (`role="tabpanel"` with `aria-labelledby`, `hidden` when inactive). No `@radix-ui/react-tabs` dep. Reuse for any future tabbed UI; the IDs are generated from `React.useId()` so multiple `<Tabs>` on a page don't collide.
- Feature card → simulation context wiring convention: each feature card calls `useSimulation()` to read upstream inputs (e.g. `financing`) AND publishes its own input back to the context via `useEffect` (e.g. `setExtraMonthly(...)` when `isValid && extraMonthly !== null`). Heavy derivations (engine calls) live in `useMemo` keyed on `[financing, ...localInputs]`. This keeps reactive recompute cheap and lets downstream cards (results / charts / export) consume one canonical state.
- Form → engine adapter pattern: every feature card converts `FinancingFormValues` (plain numbers, percent units, integer term) into `FinancingInputs` (Decimal principal, Decimal monthlyRate as fraction, integer term, optional `system`). Keep the adapter local (`buildFinancingInputs`) until a second card needs it, then promote to a shared util. Always: `principal = propertyValue − downPayment`, `monthlyRate = monthlyRatePct / 100`.
- When publishing form state to context with `mode: "onChange"`, gate the publish on `isValid` from `formState`: invalid → `setX(null)`. This prevents intermediate keystrokes (e.g. `null`, `0`, NaN) from leaking into downstream simulation derivations, but still recomputes reactively as soon as the form passes Zod validation.
- Theme palette (tweakcn `index.css`) is all reds/oranges — there is no `--success`/`--info`/green/blue token. For semantic indicator chips (e.g. the scenario badge in `RentVsBuyCard`), use literal Tailwind utility colors with dark-mode variants (`bg-emerald-500/15 text-emerald-700 dark:text-emerald-400`, `bg-sky-500/15 text-sky-700 dark:text-sky-400`). The "no hardcoded colors" rule in CLAUDE.md is scoped to card surfaces (`bg-card`/`text-card-foreground`/`border-border`), not every element.
- "Optional with min(0)" form fields: default to `0` in `defaultValues`, not `null`. `CurrencyInput`/`PercentageInput` return `null` when blank, which would fail the schema's `required_error`. Default `0` keeps the form valid when the user skips the field, while still surfacing a non-negative error if they type a negative.
- Unit-switcher pattern (e.g. months ↔ years on a horizon field): store the canonical value (months) in the form, keep the unit in **local React state outside the schema**, render a derived display value (`months` or `months/12`), and re-validate via `setValue(fieldName, canonicalValue, { shouldValidate: true })` after toggling. Don't add the unit to the schema — keep the schema engine-shaped.
- Publishing-to-context tests: when a card publishes state via `useEffect` gated on `isValid`, write the state-assertion inside its **own** `waitFor`. The error message renders synchronously (Zod sync), but the `useEffect` that mirrors the validity into context only runs after the next commit — a single `waitFor` on the error can race the context update.
- `useEffect` / `useMemo` deps for RHF watch: list `watched.fieldName` for **each** field instead of the whole `watched` object. `useWatch` returns a fresh object each render, so a dep on `watched` would fire on every render. Mirrors `ExtraPaymentCard` / `RentVsBuyCard`.
- When a user story "Replaces the X placeholder in App.tsx", also adjust any `App.test.tsx` placeholder counters (`getAllByText(/preencha os dados para simular/i).length`). Each replacement drops the count by 1. Easy to forget because the test name doesn't mention the feature being added.
- Simulation context tracks four pieces of state (US-013 added the 4th): `financing`, `extraMonthly`, `extraStrategy` ("term" | "installment", default "term"), and `rentVsBuy`. `ExtraPaymentCard` owns the tabs UI and publishes the active strategy to context via controlled `<Tabs value={extraStrategy} onValueChange={setExtraStrategy}>`. Downstream cards (results / charts / export) read it back through `useSimulation()` — never fork tabs locally for "which strategy is selected".
- Metric-cell anti-pattern: a `<div><dt>Label</dt><dd>Value</dd></div>` metric's `textContent` is `"LabelValue"` (no separator). For equal-value assertions across two metrics with different labels, compare `.querySelector("dd")?.textContent`, not the outer `textContent`. Same pitfall will hit US-014 amortization table rows.
- Delta coloring without a `--success` token: render the value inside a wrapper carrying `data-delta-sign={sign > 0 ? "positive" : sign < 0 ? "negative" : "zero"}` and apply tone classes (`text-emerald-600 dark:text-emerald-400` / `text-destructive` / `text-foreground`) to the inner `<dd>`. Tests assert via `data-delta-sign` instead of class names so the styling is free to change.
- Hand-rolled data table pattern (US-014, since `@tanstack/react-table` is not a dependency and DiceUI is unreachable offline): a single component owns `useState` for `scenario`, `page`, `sort: {columnId, direction} | null`, `filter: {from, to}`, `hiddenColumns: Set<ColumnId>`. Derivation chain: `allRows` (engine call, memoized on inputs) → `filteredRows` (month-range) → `sortedRows` → `pageRows`. Reset `page` to 0 via `useEffect` when scenario/filter/dataset changes — otherwise the user can land on an out-of-range page. For sort comparators on `Decimal` values, `.toNumber()` is fine (precision loss past ~15 digits doesn't affect ordering of monetary values). Reuse this skeleton for any future data view; lifting it to a generic `useDataTable` hook only when a second consumer needs it.
- Lightweight column-visibility toggle: `<details><summary>` with `<input type="checkbox">` rows inside. No popover primitive needed, no JS state for open/close, fully keyboard-accessible. Position the dropdown panel with `absolute z-10 mt-1` inside `<details>` so it floats over the table. Same trick works for any small "menu of toggles" UI; reach for it before pulling in `@radix-ui/react-popover`.
- Row decoration helper for the amortization table: keep a single `TableRow` shape that unifies `ScheduleRow` (base) and `PrepaymentScheduleRow` (with-extra). For base rows: `baseInstallment = installment`, `extraPayment = ZERO`. Accumulators (`cumulativeInterest`, `cumulativeAmortization`) are computed in the same map pass. The final row's status is `"Última parcela"`. This decorator is the seam US-019 (Excel export) will reuse — keep the engine returning bare schedules and the table-shaped rows here so the export can call the same function.

## 2026-05-15 - US-001 — Project scaffolding, dependencies, and CI
- Initialized Vite + React 19 + TS project at repo root with Bun.
- Installed runtime deps (decimal.js, react-hook-form, zod, @hookform/resolvers, recharts, xlsx, @fontsource/{poppins,libre-baskerville,ibm-plex-mono}, lucide-react, clsx, tailwind-merge, class-variance-authority, @radix-ui/react-slot).
- Installed dev deps (vitest 3, @testing-library/{react,jest-dom}, jsdom, eslint 9, typescript-eslint, @tailwindcss/vite, tailwindcss v4, @vitejs/plugin-react, globals, eslint-plugin-react-{hooks,refresh}).
- Configured strict TS (`tsconfig.json` references `tsconfig.app.json` + `tsconfig.node.json`; both have `strict: true` and `noImplicitAny: true`).
- Configured ESLint flat config with `no-explicit-any: error`.
- Created folder structure under `src/`: `app/`, `core/finance/`, `features/simulator/{components,hooks,schemas}/`, `components/{ui,finance}/`, `lib/{export,storage,formatters}/`, `types/`, `tests/`.
- Copied `/index.css` (tweakcn theme) into `src/index.css` so Vite can serve it (Vite needs the entry CSS inside `src/`). The root `/index.css` is preserved as-is per the PRD reference.
- Added `components.json` for shadcn/ui (style: new-york).
- Added `public/_redirects` (`/* /index.html 200`) for Cloudflare Pages SPA fallback.
- Added `.github/workflows/ci.yml` with steps: install -> typecheck -> lint -> test -> build (using oven-sh/setup-bun).
- Verified `bun run dev` boots on http://localhost:5173 and renders shell.
- Files changed: `package.json`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `vite.config.ts`, `eslint.config.js`, `components.json`, `index.html`, `.gitignore`, `public/_redirects`, `.github/workflows/ci.yml`, `src/main.tsx`, `src/App.tsx`, `src/index.css`, `src/vite-env.d.ts`, `src/lib/utils.ts`, `src/tests/setup.ts`, `src/tests/smoke.test.ts`, `bun.lock`.
- **Learnings for future iterations:**
  - Vitest 2.x bundles its own Vite 5, which causes type-conflicts against Vite 6 from the root. Use Vitest ^3 with Vite 6 to keep `defineConfig` types compatible. If you ever bump Vite, bump Vitest in lockstep.
  - Bun isn't preinstalled in this container — install it with `npm i -g bun` (the official curl installer cannot reach bun.sh from here).
  - Don't write a separate `vitest.config.ts`; merge `test` into `vite.config.ts` and import `defineConfig` from `vitest/config`. Otherwise you'll hit `Object literal may only specify known properties, and 'test' does not exist` during `tsc`.
  - The theme CSS at `/workspace/index.css` is the tweakcn source — copy/symlink into `src/index.css`, don't rewrite. Tailwind v4 expects the `@import "tailwindcss"` + `@theme inline` syntax already in place.
  - The acceptance criteria mention `bun create vite` but it's interactive and can't run here; hand-rolling `package.json` + configs is equivalent and avoids the prompt.
  - Lint ignores `src/components/ui/**` because shadcn-generated code occasionally trips the `react-refresh/only-export-components` rule.
---

## 2026-05-15 - US-002 — BRL currency, percentage, and number formatters
- Added shared pt-BR formatters under `src/lib/formatters/`:
  - `currency.ts` — `formatBRL(value: Decimal | number)` returns `"R$ 1.234,56"` (Intl currency, NBSP normalized to space).
  - `percentage.ts` — `formatPercentage(value, decimals = 2)` returns `"1,25%"` (value is in percent units; internally divided by 100). `parsePercentage(input)` is the inverse and tolerates optional `%`, whitespace, and pt-BR thousand/decimal separators.
  - `number.ts` — `formatInteger(value)` returns `"1.234.567"`. `formatMonths(months)` returns `"6 meses"` / `"1 mês"` for < 12, `"Y anos e Z meses"` (with singular handling) otherwise.
- Added Vitest suites alongside each formatter covering zero, small/large positive, negative, fractional rounding, Decimal input, custom decimals, and the percentage round-trip required by the AC.
- Files changed: `src/lib/formatters/currency.ts`, `src/lib/formatters/currency.test.ts`, `src/lib/formatters/percentage.ts`, `src/lib/formatters/percentage.test.ts`, `src/lib/formatters/number.ts`, `src/lib/formatters/number.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (39 tests across 4 files), `bun run build` ✅.
- **Learnings for future iterations:**
  - `Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" })` joins the `R$` symbol to the number with U+00A0 (or U+202F on newer ICU), not a regular space. Strip it (e.g. `new RegExp("[\\u00A0\\u202F]", "g")` then replace with `" "`) so test assertions like `"R$ 1.234,56"` match. Do the same in any future formatter that wraps Intl currency/percent output.
  - Don't write a regex literal that contains a literal NBSP — ESLint `no-irregular-whitespace` blocks it. Build the RegExp from a string with ` ` / ` ` escape sequences instead.
  - Negative BRL is rendered as `"-R$ 1.234,56"` (sign before the symbol), not `"R$ -1.234,56"` — relevant for any future test snapshots.
  - The percent-style Intl formatter multiplies by 100 internally, so a "value-in-percent-units" API (preferred for human-friendly inputs like a 1,25% rate field) needs to divide by 100 before calling `.format()`.
  - Round-trip tests for `parsePercentage(formatPercentage(x))` use `toBeCloseTo(x, 2)` because the formatter rounds to the configured decimal count — strict equality won't hold for inputs like `1.234`.
---

## 2026-05-15 - US-003 — PRICE financial engine (installment + schedule)
- Added `src/core/finance/financial-types.ts` defining `AmortizationSystem`, `FinancingInputs`, `ScheduleRow`, `ScheduleSummary`, plus a one-shot `Decimal.set({ precision: 30, rounding: ROUND_HALF_EVEN })` side-effect, a `roundMoney` helper, and a `summarizeSchedule` aggregator.
- Added `src/core/finance/price-calculator.ts` exporting `calculatePriceInstallment({ principal, monthlyRate, termMonths })` using the standard PRICE closed-form formula (with a zero-rate branch returning `principal/term`). Validates non-negative principal/rate and positive term.
- Added `src/core/finance/amortization.ts` exporting `generatePriceSchedule(inputs)` that rounds the base installment once, recomputes interest from the running balance each month, and lets the final installment absorb any rounding residual so the closing balance is exactly 0.
- Tests cover: 360-month golden fixture (R$ 300.000 @ 0,8%/mês → first installment R$ 2.544,48; final balance 0; total amortization = principal); 1-month edge case; zero-rate edge case; balance-zero invariant for 4 additional `(principal, rate, term)` combinations; `interest + amortization === installment` for every row; monotonic-decreasing balance; calculator throws on invalid inputs; `summarizeSchedule` aggregates correctly.
- Files changed: `src/core/finance/financial-types.ts`, `src/core/finance/price-calculator.ts`, `src/core/finance/price-calculator.test.ts`, `src/core/finance/amortization.ts`, `src/core/finance/amortization.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (62 tests across 6 files), `bun run build` ✅.
- **Learnings for future iterations:**
  - Configure Decimal.js once per process. I put the `Decimal.set` call at module scope in `financial-types.ts`; every other finance module imports from there, which guarantees the precision/rounding is applied before any `Decimal` math runs in the engine. Don't sprinkle `Decimal.set` calls in feature code.
  - PRICE base installment formula: `P · i · (1+i)^n / ((1+i)^n − 1)`. With `i = 0`, the formula is undefined (0/0); explicitly branch to `P/n`. Keep this branch — Zod allows rate > 0 in the form (US-007), but the engine must still tolerate `0` so callers can compose it with future rate-curve features.
  - Use **bankers' rounding** (`Decimal.ROUND_HALF_EVEN`) consistently. Decimal.js defaults to `ROUND_HALF_UP`, which can drift on long schedules; the AC explicitly requires HALF_EVEN.
  - The "final installment absorbs residual" pattern: in the loop, when `month === termMonths`, set `amortization = balance`, `installment = roundMoney(balance + interest)`, then `balance = 0`. This makes `sum(amortization) === principal` and `last.balance === 0` exact, which simplifies downstream charts/exports.
  - `Decimal#toString()` does **not** preserve trailing zeros: `new Decimal("10100.00").toString() === "10100"`. Don't compare to `"10100.00"` in tests — use `.equals()` or `.toFixed(2)`.
  - When you need a quick golden fixture, write the implementation first then `bun -e "import('./src/core/finance/...').then(...)"` to compute the actual rounded value, and lock that into the test. Faster and more accurate than mental log/exp math.
  - Empty-schedule guard (`termMonths <= 0` → `[]`) makes the function safer for UI code that might bind to a half-filled form before validation runs.
---

## 2026-05-15 - US-004 — SAC financial engine (installment + schedule)
- Added `generateSacSchedule(inputs)` to `src/core/finance/amortization.ts`. Mirrors the PRICE row shape (`ScheduleRow`) and reuses the same `roundMoney` helper + `final-row absorbs residual` pattern, so PRICE and SAC have identical output contracts and zero-balance guarantees.
- Algorithm: base amortization = `roundMoney(principal / termMonths)`, constant for rows `1..n-1`. For each month: `interest = roundMoney(balance * monthlyRate)`, `installment = roundMoney(amortization + interest)`, `balance = balance - amortization`. The final row sets `amortization = balance` so the closing balance lands exactly at 0 and `sum(amortization) === principal`.
- Tests added to `src/core/finance/amortization.test.ts` cover the 360-month golden fixture (first installment R$ 3.233,33, juros R$ 2.400, amort R$ 833,33, balance R$ 299.166,67; second-row regression for monotonic decrease; constant amortization invariant for rows 1..n-1; installment non-increasing; interest strictly decreasing; final balance = 0; `interest + amortization === installment`; total amort = principal), 1-month edge case, zero-rate edge case, balance-zero invariant on 4 (principal, rate, term) combos, and the `sum(SAC interest) < sum(PRICE interest)` invariant on 4 combos.
- Files changed: `src/core/finance/amortization.ts`, `src/core/finance/amortization.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (83 tests across 6 files, 21 new SAC cases), `bun run build` ✅.
- **Learnings for future iterations:**
  - SAC installments are NOT strictly monotonic in pathological corners — with rounded constant amortization, the final residual row's amortization may be slightly larger than the base, and zero-rate cases collapse to all-equal installments. Write the test as `lessThanOrEqualTo` ("non-increasing"), not `lessThan`. The PRD AC text "decreases monotonically" describes the algorithmic intent, not a strict test assertion.
  - SAC residual is bounded: with HALF_EVEN rounding to 2dp, the per-month rounding error is ≤ 0.005, so over `n` months the residual is ≤ `n * 0.005`. For typical `(P, r, n)` the interest drop dominates the residual growth, so the final installment still ends up lower than the previous one — but don't rely on it for tests, use the non-strict comparator.
  - Generating the golden fixture: run `bun -e "..."` against the engine to lock in the actual rounded numbers (`first installment 3233.33`, `last installment 841.21`, `total interest 433201.72` for 300k @ 0,8% × 360). Same pattern as US-003.
  - The `sum(SAC interest) < sum(PRICE interest)` invariant is the cleanest cross-system sanity check. It holds for any (P > 0, r > 0, n > 1) because SAC pays principal faster, shrinking the interest base. Useful as a regression guard whenever either schedule changes.
  - Reuse `ScheduleRow` / `roundMoney` / final-row-residual pattern verbatim — don't fork a SAC-specific row type. Downstream consumers (prepayment, charts, table, export) can treat PRICE and SAC schedules interchangeably.
---

## 2026-05-15 - US-005 — Prepayment engine (reduce-term + reduce-installment)
- Added `src/core/finance/prepayment.ts` exporting `applyPrepaymentReduceTerm(inputs, extra)` and `applyPrepaymentReduceInstallment(inputs, extra)`. Both accept `FinancingInputs` (PRICE | SAC via `system`) and a Decimal extra, return `{ schedule, summary }`.
- `PrepaymentScheduleRow` extends `ScheduleRow` with `baseInstallment` (what the row's installment would be without extra) and `extraPayment` (extra applied that month). `installment` is the total cash outflow; final partial rows recover `baseInstallment + extraPayment === installment` via a `splitFinalPayment` helper that caps recorded extra at the user's nominal extra.
- `PrepaymentSummary` carries totalPaid, totalInterest, interestSaved (vs base of the same system), originalTermMonths, newTermMonths, monthsReduced.
- Reduce-term: pay base + extra each month; PRICE base installment is constant, SAC base amortization is constant; loop ends when balance reaches 0 (term shortens) or `month === termMonths` (acts as a safety net for the extra=0 case where rounding residuals fall outside the `desiredTotal >= owed` window).
- Reduce-installment: each month recompute the base installment from (current balance, remaining-original-term). User pays recomputed-base + extra. With recurring extras the loan still finishes earlier than the original term (mathematically unavoidable — the PRD's "term fixed" reflects the recalculation rule, not the actual outcome). With extra=0 we short-circuit to the canonical base schedule to avoid cent-level rounding drift from the recursive PRICE/SAC recalculation.
- Tests cover the 4 (strategy × system) combinations with invariants (`balance` zero at end, `sum(amortization) === principal`, `interest + amortization === installment`, interestSaved > 0, installment-shape per strategy), plus zero-extra passthrough that matches `generatePriceSchedule` / `generateSacSchedule` row-for-row for both strategies, plus a cross-strategy invariant (`reduce-term ends earlier and saves more than reduce-installment for the same extra`), plus validation throws.
- Files changed: `src/core/finance/prepayment.ts`, `src/core/finance/prepayment.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (115 tests across 7 files, 32 new prepayment cases), `bun run build` ✅.
- **Learnings for future iterations:**
  - "Term fixed" for reduce-installment is mathematically impossible with recurring extra payments under any standard convention. The natural interpretation is: keep recalculating the installment as if the original term still applied, and accept that the actual term naturally shortens because cumulative prepayments outpace the remaining schedule. Document this in the engine and test for `newTermMonths <= originalTermMonths`, not strict equality.
  - For row-level base-vs-extra accounting on the **final** (partial) row, the formula-based `rowBaseInstallment` can disagree with the actual installment (`balance + interest`) in either direction — SAC's final residual row often exceeds `baseAmort + interest` by a cent or two. Pass the user's `extraCap` into the split helper and clamp recorded extra to `min(surplus, extraCap)` so the `extra=0` passthrough doesn't accidentally allocate residual cents to "extra".
  - Always combine `month === termMonths` with the "would-clear-balance" heuristic as the final-month trigger. The heuristic alone misses SAC's final-month residual when `balance > baseAmortization`. Same fix the base `generateSacSchedule` uses — keep the patterns aligned.
  - The vitest matcher is `toBeLessThanOrEqual` (Jest-style), not `toBeLessThanOrEqualTo`. Easy to mis-type since Decimal.js exposes `.lessThanOrEqualTo`. If you write the wrong one Vitest throws "Invalid Chai property" with a helpful suggestion.
  - When you need to compare a prepayment schedule row-by-row against a base schedule, watch for the `extraPayment` field: even with `extra=0`, the schedule still carries the field. Assert `row.extraPayment.equals(0)` explicitly — it's a useful sentinel.
  - Reuse the base schedule generators (`generatePriceSchedule` / `generateSacSchedule`) inside prepayment to compute the baseline interest figure for `interestSaved`. Don't recompute the base interest in a custom loop; the existing engines are authoritative and reuse keeps the savings number consistent with whatever the user sees in the base scenario.
---

## 2026-05-15 - US-006 — Rent vs. buy comparison engine
- Added `src/core/finance/rent-vs-buy.ts` exporting `compareRentVsBuy(inputs): { buyTimeline, rentTimeline, summary }`, plus a reusable `annualToMonthlyRate(annual)` helper (`(1 + a)^(1/12) − 1` via Decimal.js `pow`).
- Inputs are flat (`propertyValue, downPayment, monthlyRate, termMonths, system?, monthlyRent, annualRentAdjustment, annualInvestmentReturn, annualAppreciation, monthlyOwnershipCosts, horizonMonths`). Schedule is built by reusing the PRICE/SAC engines (`generatePriceSchedule` / `generateSacSchedule`) — no duplicated finance logic.
- Buy timeline rows: `{ month, propertyValue, outstandingBalance, investedCapital, netWorth }`. Rent timeline rows: `{ month, rent, investedCapital, monthlyContribution, netWorth }`. Both timelines include month 0 (starting state) so length is `horizonMonths + 1`.
- Symmetric monthly model: the lower-spending side invests `|buyOutflow − rentOutflow|`; the higher spender contributes nothing (no borrowing). Buy outflow = installment + ownership costs; after `termMonths` the installment drops to zero. Both portfolios grow by the monthly investment rate first, then the contribution is added (so the new contribution doesn't earn interest in its own month).
- Property value compounds monthly via the same `annualToMonthlyRate` helper. Rent steps up at month 12, 24, 36, ... by `(1 + annualRentAdjustment)`.
- Summary: `bestScenario ('buy' | 'rent' | 'tie')`, `netWorthDifferenceFinal` (Decimal, `buy − rent`), `breakEvenMonth` (first m ≥ 1 where `buy.netWorth ≥ rent.netWorth`, else `null`). Tie tolerance is R$ 0.01.
- Tests cover all five PRD-required scenarios plus invariants and validation: equal-outcome (zero rates + free rent → tied at every month), buy-wins (high appreciation), rent-wins + no break-even (high return, no appreciation), break-even with crossover at m=44 (10% appreciation vs 6% return), timeline length, m=0 starting positions, monthly-contribution non-negativity, rent step-up at m=12/24, geometric appreciation, post-term horizon (loan paid off, only ownership costs continue), SAC system support, and 8 input-validation throw cases.
- Files changed: `src/core/finance/rent-vs-buy.ts`, `src/core/finance/rent-vs-buy.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (145 tests across 8 files, 30 new rent-vs-buy cases), `bun run build` ✅.
- **Learnings for future iterations:**
  - To reliably get a mid-horizon break-even crossover, the appreciation rate must exceed the investment return AND the buy-side outflow (installment + ownership) must initially exceed rent. With those parameters, the renter's portfolio grows faster early (renter is invested + receiving contributions) but the buyer's property growth eventually overtakes it. If `appreciation ≤ return`, buy will never catch up to a renter who started ahead. Useful for crafting deterministic test scenarios.
  - For the equal-outcome ("tie") test: zero rates everywhere + monthlyRent = 0 means each month the renter's contribution = the buyer's installment = that month's amortization (since rate = 0 → installment = principal/n). Both portfolios then grow by the same per-month amount and tie exactly at every m. This is the cleanest deterministic tie scenario; constructing one with non-zero rates is much harder due to compound-interest asymmetry.
  - `Decimal#pow(Decimal#div(...))` works for fractional exponents (verified `(1.12)^(1/12)` against expected ≈ 1.00949 within Decimal-precision tolerance). No need to fall back to `Math.pow` — keep everything in Decimal so precision stays controlled by the global `Decimal.set` from `financial-types.ts`.
  - The order "grow then contribute" (vs "contribute then grow") matters for one-month results: with "grow then contribute", a brand-new contribution doesn't earn interest in the same month. This matches typical end-of-month savings convention. Don't change without intent — it shifts every result.
  - PRD asymmetry: buy timeline doesn't include `monthlyContribution` while rent timeline does. Track the buyer's contribution internally (you need it for the buyer's invested capital) but don't expose it. Mirroring the AC field list exactly avoids unused-property noise downstream.
  - Validation-throw tests for finance engines: `expect(() => fn(badInputs)).toThrow(/fieldName/)` — the regex matches because the engine's error messages embed the field name (e.g. `"propertyValue must be greater than zero"`). Keeps tests resilient to small wording tweaks.
---

## 2026-05-15 - US-007 — Zod input validation schemas in pt-BR
- Added three Zod schemas under `src/features/simulator/schemas/` with pt-BR error messages, ready to wire into React Hook Form via `zodResolver`:
  - `financing.ts` — `financingSchema` (propertyValue > 0, downPayment ≥ 0 AND < propertyValue via `.refine`, termMonths positive integer, monthlyRate > 0, system enum `PRICE | SAC`). Also exports `amortizationSystemSchema`, `FinancingFormValues`, `AmortizationSystemValue`.
  - `prepayment.ts` — `prepaymentSchema` (extraMonthly ≥ 0). Exports `PrepaymentFormValues`.
  - `rent-vs-buy.ts` — `rentVsBuySchema` (monthlyRent ≥ 0, annualRentAdjustment ≥ 0, annualInvestmentReturn ≥ 0, annualAppreciation ≥ 0, monthlyOwnershipCosts ≥ 0, horizonMonths positive integer). Exports `RentVsBuyFormValues`.
- Every numeric field includes `required_error`, `invalid_type_error`, `.finite(...)`, and a range constraint — all messages in pt-BR (e.g. "O valor do imóvel deve ser maior que zero", "A entrada deve ser menor que o valor do imóvel").
- Added Vitest suites covering at least one valid + one invalid case per field per schema, including: zero, negative, non-numeric, missing, non-integer (where applicable), cross-field refine (downPayment ≥ propertyValue), and unknown enum values. Tests use a `firstErrorFor(result, path)` helper that resolves nested + refine paths consistently.
- Files changed: `src/features/simulator/schemas/financing.ts`, `src/features/simulator/schemas/financing.test.ts`, `src/features/simulator/schemas/prepayment.ts`, `src/features/simulator/schemas/prepayment.test.ts`, `src/features/simulator/schemas/rent-vs-buy.ts`, `src/features/simulator/schemas/rent-vs-buy.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (184 tests across 11 files, 39 new schema cases), `bun run build` ✅.
- **Learnings for future iterations:**
  - Zod 3.25 (installed via `zod@^3.24.1`) still ships the v3 classic API by default. `z.number({ required_error, invalid_type_error })` is the right pattern for pt-BR overrides on every numeric form field. The v4 API is reachable via `zod/v4` but unused here — don't accidentally import from it.
  - Cross-field rules (e.g. `downPayment < propertyValue`) go in `.refine(...)` with `path: ["fieldName"]` so React Hook Form surfaces the error below the correct input. Without `path`, the issue lands at the root which most form components silently swallow.
  - Don't rely on `error.flatten().fieldErrors[field][0]` in tests — `.refine` errors that target a single field do land in `fieldErrors`, but only after Zod's flatten pass, and the per-field ordering isn't guaranteed across versions. Using `error.issues.find((i) => i.path.join(".") === path)?.message` reads the same as RHF will resolve it and is order-stable.
  - For "missing value" tests, destructure-and-omit: `const { propertyValue: _omit, ...rest } = baseValid; void _omit;`. The `_omit` rename satisfies `argsIgnorePattern: "^_"`, but the ESLint `no-unused-vars` rule still flags the binding as unused unless you reference it once (`void _omit;`). Don't disable the rule per-file.
  - Schemas validate the **form layer** (plain numbers). Engine inputs are Decimal-typed (`FinancingInputs` etc.). The form/useSimulation hook (US-010/US-011/US-012) is responsible for converting between the two — keep the schemas number-typed so RHF's resolver works out of the box.
  - `.gt(0, ...)` and `.min(0, ...)` are distinct: `> 0` rejects zero, `≥ 0` accepts it. Map carefully to each AC: propertyValue/termMonths/monthlyRate/horizonMonths use `gt(0)`; downPayment/extraMonthly/rent/all-percentages/ownership use `min(0)`.
  - Add `.finite(...)` after `z.number()` to reject `Infinity`/`-Infinity`/`NaN` with a pt-BR message — useful when a future CurrencyInput accidentally parses something weird. The default `z.number()` already rejects `NaN` but not Infinity.
---

## 2026-05-15 - US-008 — Theme integration (index.css, self-hosted fonts, dark mode toggle)
- Wired self-hosted fonts in `src/main.tsx` (`@fontsource/poppins` 400/500/600/700, `@fontsource/libre-baskerville` 400/700, `@fontsource/ibm-plex-mono` 400/500). Existing `@theme inline` block in `src/index.css` already exposes `--font-sans`, `--font-serif`, `--font-mono` as Tailwind v4 design tokens — no Tailwind config change needed.
- Added `.font-tabular` utility class to `src/index.css` (under `@layer utilities`) — uses `var(--font-mono)` + `font-variant-numeric: tabular-nums` for monetary cells. Also set `body { @apply ... font-sans }` so the default text uses Poppins out of the box.
- Added a shadcn-styled `Button` (new-york, neutral baseColor) at `src/components/ui/button.tsx` — hand-rolled since the shadcn CLI can't reach the network in this sandbox. ESLint already ignores `src/components/ui/**` so the shadcn idioms (`forwardRef`, `cva` variants, `displayName`) pass cleanly.
- Added theme system across two files:
  - `src/components/theme-context.ts` — `createContext`, `ThemeContextValue` / `Theme` types, and the `useTheme()` consumer hook.
  - `src/components/theme-provider.tsx` — `<ThemeProvider>` component. Initial mode reads `window.matchMedia('(prefers-color-scheme: dark)')`. Subscribes to `change` events to follow OS theme until the user toggles, at which point an in-memory `userOverridden` flag stops re-syncing for the rest of the session. No `localStorage` / no persistence per the AC. The provider's `useEffect` adds/removes the literal `"dark"` class on `document.documentElement` (matches the `@custom-variant dark (&:is(.dark *))` and `.dark { ... }` overrides already in `src/index.css`).
- Added `src/components/theme-toggle.tsx` — shadcn `Button variant="ghost" size="icon"` with a `Sun`/`Moon` `lucide-react` icon and pt-BR `aria-label` (`"Ativar modo escuro"` / `"Ativar modo claro"`).
- Rendered `<ThemeProvider>` around `<App />` in `src/main.tsx` (inside `StrictMode`). Mounted `<ThemeToggle />` in a small header in `src/App.tsx` so the toggle is reachable, plus a sample `.font-tabular` BRL string for a quick visual check that the mono font is wired up.
- Tests added in `src/components/theme-provider.test.tsx` (6 cases): initial mode = light when prefers-color-scheme is light; initial mode = dark when prefers-color-scheme is dark; toggling flips `.dark` on `<html>`; provider follows OS preference until user overrides, then stops following; `setTheme('light' | 'dark')` switches to exact mode; `useTheme()` throws outside provider. jsdom doesn't ship `matchMedia` — stubbed via `Object.defineProperty(window, "matchMedia", ...)` returning a fake `MediaQueryList` that tracks listeners and exposes a `triggerChange()` helper.
- Browser verification: `bun run dev` boots cleanly on http://localhost:5173, the HTML response is intact (`curl` check), and the runtime DOM behavior the AC requires (`.dark` class toggle on `<html>`, prefers-color-scheme initial mode, override stickiness) is asserted by the jsdom tests. No headless browser tool available in this sandbox for an interactive light/dark screenshot — recommend a manual dev-browser pass before merging if visual regression matters; the underlying mechanism is fully covered by the unit suite.
- Files changed: `src/main.tsx`, `src/App.tsx`, `src/index.css`, `src/components/theme-context.ts`, `src/components/theme-provider.tsx`, `src/components/theme-provider.test.tsx`, `src/components/theme-toggle.tsx`, `src/components/ui/button.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅ (zero warnings after splitting context out of the provider file), `bun run test` ✅ (190 tests across 12 files, 6 new theme cases), `bun run build` ✅.
- **Learnings for future iterations:**
  - Tailwind v4 + this tweakcn theme: the `@theme inline { --font-sans: var(--font-sans); ... }` block (already in `src/index.css`) is what surfaces the CSS vars as Tailwind utilities (`font-sans`, `font-mono`, `font-serif`). You don't add fonts in a `tailwind.config.js` — just import the `@fontsource/...` CSS files in `src/main.tsx` and the existing `@theme inline` block picks them up. New utilities go in `@layer utilities` in `src/index.css` (e.g. `.font-tabular`).
  - The toggling class must be `dark` applied to `document.documentElement` (i.e. `<html>`), not `<body>` or a custom name. `src/index.css` defines `@custom-variant dark (&:is(.dark *))` plus a `.dark { ... }` block of var overrides; deviating from `dark` on `<html>` would silently neutralize every theme variable.
  - When a `.tsx` file would export both a component AND a hook/context/constant, `eslint-plugin-react-refresh/only-export-components` warns (Fast Refresh boundary issue). Split the consumer hook + `createContext` call + types into a sibling `.ts` (no JSX) file and keep only the component export in the `.tsx`. Apply this pattern for every future provider (e.g. simulation-state context).
  - jsdom doesn't implement `window.matchMedia`. Stub via `Object.defineProperty(window, "matchMedia", { configurable: true, writable: true, value: ... })` — `vi.stubGlobal("matchMedia", ...)` alone is insufficient because `matchMedia` on `window` is non-writable by default in jsdom. Return a fake `MediaQueryList` that captures the listener so you can fire synthetic `change` events.
  - shadcn CLI (`bunx shadcn@latest add <name>`) needs network access (https://ui.shadcn.com). In this sandbox it fails with "Unable to connect". Hand-roll components into `src/components/ui/<name>.tsx` — ESLint already ignores that folder so the typical shadcn idioms (`React.forwardRef`, `cva`, `displayName`) pass without changes. The Button source used here is the standard new-york preset; future components (`Card`, `Tabs`, `Select`, `Input`) should be added the same way.
  - "In-memory only, no persistence" for the theme override means **no `localStorage` read or write**. Tests should be free of `localStorage` setup/teardown. If the AC ever changes to "remember across reloads," add it as a separate effect rather than wiring it into the provider's initial-state computation, so SSR/jsdom paths stay clean.
---

## 2026-05-15 - US-009 — App shell (single-page layout with section cards)
- Moved the app entry from `src/App.tsx` to `src/app/App.tsx` (matches the PRD AC) and updated the import in `src/main.tsx` to `@/app/App`.
- Hand-rolled the shadcn `Card` primitive at `src/components/ui/card.tsx` (new-york style, `forwardRef` + `cn` + `displayName`; exports `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`). `CardTitle` is `<h3>` styled with `font-serif` so the headings inherit Libre Baskerville from the theme.
- Rebuilt the App shell with a sticky header (`sticky top-0 z-40 ... shadow-sm`) containing the pt-BR title "Simulador de Financiamento" + `<ThemeToggle />`, and a responsive content area: `flex flex-col gap-6 md:grid md:grid-cols-12 md:gap-6` for stacked-below-768px / 12-column-above-768px layout.
- Rendered eight placeholder cards in order: Premissas gerais (col-span-12), Financiamento (col-span-7), Pagamento extra (col-span-5), Aluguel vs. compra (col-span-12), Resumo dos resultados (col-span-12), Tabela de amortização (col-span-12), Gráficos (col-span-12), Exportar (col-span-12). Each one uses a tiny local `PlaceholderCard` helper that renders `<Card>` (which already applies `bg-card text-card-foreground border-border` so the AC's "use theme tokens — no hardcoded colors" is satisfied) with a pt-BR title, a short pt-BR description, and an empty-state body ("Preencha os dados para simular" for inputs/results, "Informe um valor extra para simular" for extra payment, "Preencha os dados para habilitar a exportação" for export).
- Added `src/app/App.test.tsx` (6 cases) that asserts: header h1 + theme-toggle button render; the eight section h3 titles render in the exact PRD order; pt-BR empty-state copy is present (≥ 5 base instances + the two specific variants); every `Card` carries `bg-card`, `text-card-foreground`, and `border-border` classes; the header has the `sticky`/`top-0`/`shadow` classes; the main grid container has both `flex-col` and `md:grid md:grid-cols-12`. Uses a small `stubMatchMedia` helper to satisfy `<ThemeProvider>`'s `window.matchMedia` call under jsdom.
- Browser verification: `bun run dev` boots cleanly on http://localhost:5173 (HTTP 200, valid `<!doctype html>` with `<title>Simulador Financeiro</title>` and `<div id="root">`). All AC-required structure (sticky header, 12-col grid, eight ordered placeholder cards with theme tokens) is covered by the jsdom unit suite. No headless browser tool available in this sandbox to capture a light/dark screenshot — recommend a manual dev-browser pass before merging if visual regression matters; the underlying structure is fully covered by unit tests.
- Files changed: `src/app/App.tsx` (new), `src/app/App.test.tsx` (new), `src/components/ui/card.tsx` (new), `src/main.tsx` (import path), `src/App.tsx` (deleted), `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (196 tests across 13 files, 6 new App-shell cases), `bun run build` ✅ (1m 27s; final bundle 226.76 kB JS / 31.98 kB CSS, gzip 71.61 / 6.26).
- **Learnings for future iterations:**
  - The PRD's project structure expects the app entry at `src/app/App.tsx`, not `src/App.tsx`. Move and update `src/main.tsx`'s import (`import App from "@/app/App"`). Future page-level wiring (`useSimulation` context provider, the form-state hook from US-010/011/012) belongs under `src/app/`, not under `src/features/simulator/`.
  - shadcn primitives go in `src/components/ui/`. After this story we have `button.tsx` and `card.tsx`. Always hand-roll (no network in the sandbox) using the new-york preset. ESLint ignores the folder so `React.forwardRef`, `cn(...)`, and `displayName` patterns pass without changes. The next stories (US-010 onwards) will need Input, Select, Tabs — same drill.
  - Reuse `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent` across every feature card. Don't redefine the visual shell per feature; just swap the body content. The PRD's "use theme tokens — no hardcoded colors" rule is enforced by the primitive (`bg-card text-card-foreground border-border`), so feature cards inherit it automatically.
  - Responsive layout convention: `flex flex-col gap-6 md:grid md:grid-cols-12 md:gap-6` on the container, then each child sets its own `md:col-span-*`. Stack vertically below 768px, 12-column grid at and above. Container is `mx-auto max-w-screen-xl px-4 py-8 md:px-6 md:py-10` so content stays comfortable on large displays.
  - Sticky header pattern: `<header class="bg-background/95 border-border supports-[backdrop-filter]:bg-background/75 sticky top-0 z-40 border-b shadow-sm backdrop-blur">`. The `z-40` keeps it above grid content; the `backdrop-blur` + 95%/75% opacity layered fallback gives a subtle translucent feel that still works when scrolled content is behind. Don't increase opacity past 95% — the AC requires "a subtle shadow," and at 100% opacity the shadow looks heavy because the background is solid.
  - Empty-state copy convention: use the canonical "Preencha os dados para simular" for input/results cards, but customize per card when the action is more specific ("Informe um valor extra para simular" for extra payment, "Preencha os dados para habilitar a exportação" for export). The PRD AC says "where appropriate" — interpret that as "default + specialize when it improves UX," not "literally identical copy on every card."
  - Test against the rendered h3 list with `screen.getAllByRole("heading", { level: 3 }).map(n => n.textContent)` and assert `toEqual(expectedOrder)`. This catches both missing sections AND wrong ordering in a single assertion — cheaper and clearer than eight separate `getByText` calls.
  - Vitest workers are unusually slow in this sandbox (the full 13-file suite took 232s end-to-end, mostly setup/environment time, not test code). Don't panic if `bun run test` looks stuck — give it 3-4 minutes before assuming a deadlock. If you need fast feedback during dev, run a single file via `bunx vitest run src/app/App.test.tsx`.
---

## 2026-05-15 - US-010 — Financing simulation form with PRICE/SAC selector
- Added shadcn primitives: `src/components/ui/input.tsx`, `src/components/ui/label.tsx`, and `src/components/ui/select.tsx`. The Select is a styled native `<select>` (no new `@radix-ui/react-select` dep — `register("system")` from RHF works on it directly).
- Added reusable finance inputs under `src/components/finance/`:
  - `currency-input.tsx` — controlled `value: number | null` + `onChange`; raw editable string on focus, BRL-formatted on blur; parses via the new `parseBRL` exported from `src/lib/formatters/currency.ts`; `aria-invalid` styling inherited from `Input`.
  - `percentage-input.tsx` — same shape but for percent units; raw editable on focus, `formatPercentage(value, decimals)` on blur; configurable `decimals` (e.g. 4 for monthly rate display).
- Added `parseBRL` to `src/lib/formatters/currency.ts` (kept formatter co-located with parser, mirroring `parsePercentage`). 6 unit tests in `currency.test.ts` plus 3 in `currency-input.test.tsx` cover canonical, blank, non-numeric, NBSP, and round-trip cases.
- Added page-level state plumbing under `src/features/simulator/hooks/`:
  - `simulation-context.ts` — `createContext`, `SimulationState`, `SimulationContextValue`, and the `useSimulation()` consumer hook (.ts to satisfy react-refresh).
  - `simulation-provider.tsx` — `<SimulationProvider>` exposing `{ financing, setFinancing }`. Wraps `<App />` in `src/main.tsx`.
- Added `src/features/simulator/components/financing-form.tsx` — RHF + zodResolver bound to `financingSchema`. Fields: Valor do imóvel (currency), Entrada (currency), Valor financiado (read-only `<output>`, derived = propertyValue − downPayment), Taxa mensal (percentage), Taxa anual equivalente (read-only `<output>`, derived = (1 + i)^12 − 1), Prazo em meses (number input via `register` with `setValueAs`), Sistema de amortização (`Select` with options PRICE and SAC). Inline pt-BR errors render below each field. Submit button disabled while `!isValid`. On submit, values are published to `setFinancing` via `useSimulation`. `mode: "onChange"` makes the submit-disabled state reactive.
- Replaced the Financiamento placeholder in `src/app/App.tsx` with a real `<Card>` hosting `<FinancingForm />`. Updated `src/app/App.test.tsx` to wrap the rendered `<App />` with `<SimulationProvider>` (matches the new provider hierarchy).
- Added `src/features/simulator/components/financing-form.test.tsx` (7 cases): renders all pt-BR labels; submit disabled while invalid; cross-field refine error surfaces ("A entrada deve ser menor que o valor do imóvel") when all other fields are filled validly; pt-BR error on zero monthly rate; valor financiado + taxa anual equivalente derive reactively; valid submission enables button AND publishes the values to `useSimulation` (asserted via a fingerprint helper that JSON-stringifies the context state); selector exposes exactly PRICE and SAC options.
- Browser verification: `bun run dev` boots cleanly on http://localhost:5173 (HTTP 200 served HTML with `<title>Simulador Financeiro</title>` and the React `<div id="root"></div>` mount point intact). All AC-required runtime behavior (controlled formatting, derived outputs, validation, submit gating, context publish) is covered by the jsdom unit suite. No headless browser tool available in this sandbox for an interactive screenshot — recommend a manual dev-browser pass before merging if visual regression matters.
- Files changed: `src/main.tsx`, `src/app/App.tsx`, `src/app/App.test.tsx`, `src/components/ui/input.tsx` (new), `src/components/ui/label.tsx` (new), `src/components/ui/select.tsx` (new), `src/components/finance/currency-input.tsx` (new), `src/components/finance/currency-input.test.tsx` (new), `src/components/finance/percentage-input.tsx` (new), `src/components/finance/percentage-input.test.tsx` (new), `src/lib/formatters/currency.ts`, `src/lib/formatters/currency.test.ts`, `src/features/simulator/hooks/simulation-context.ts` (new), `src/features/simulator/hooks/simulation-provider.tsx` (new), `src/features/simulator/components/financing-form.tsx` (new), `src/features/simulator/components/financing-form.test.tsx` (new), `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (223 tests across 16 files, 33 new cases for this story), `bun run build` ✅ (47s; bundle 355.46 kB JS gzip 111.79 / 33.56 kB CSS gzip 6.57 — jump driven by RHF + zod runtime, expected).
- **Learnings for future iterations:**
  - RHF + Zod with nullable defaults: `useForm<FormShape>({ resolver: zodResolver(schema), mode: "onChange", defaultValues })` works when `FormShape` has `number | null` fields. Do NOT pass the 3-generic form `useForm<TFieldValues, TContext, TTransformed>` — it conflicts with zodResolver's typing (zodResolver returns `ResolverResult<TFieldValues>`, not `ResolverResult<TFieldValues, TTransformed>`). If you need the strongly-typed parsed value inside `handleSubmit`, cast it (`values as unknown as FinancingFormValues`) — the resolver guarantees the schema ran successfully at runtime.
  - Zod `z.object().refine(...)` cross-field rules only fire when **every** individual field validation passes. For UI tests asserting the refine message, fill in ALL required fields with valid values before introducing the violation in the field of interest. If any sibling field is still invalid (e.g. `null` failing `.gt(0)`), the refine never executes and the message never appears. Same caveat applies to `superRefine` unless you flip it with `.superRefine` + early-return logic.
  - `react-hook-form` `Controller` is the right vehicle for the currency/percentage inputs (custom value type). Pass `value={field.value ?? null}` to handle the initial-null/default-null case. For the plain number input (prazo) and the native `<select>` (system), `register()` with `setValueAs` is simpler than `Controller`.
  - Derived read-only fields render best as `<output>` (not `readOnly <input>`): the label `htmlFor` still works with `getByLabelText`, `aria-live="polite"` announces updates to screen readers, and the element doesn't get caught by form-level focus traversal or `register`-style scans. Style with the same input box classes for visual consistency, plus the `.font-tabular` utility for monetary cells.
  - The cleanest way to publish RHF form state to a page-level context without re-rendering everything is to call `setFinancing(values)` from inside `handleSubmit`. Watching `useWatch` and pushing to context on every keystroke is tempting but causes cascading re-renders of every downstream card and is hard to invalidate when fields go invalid mid-typing. Use submit as the explicit publish point; use `useWatch` only for *intra-form* derived fields (the financed-amount / annual-rate outputs).
  - When a hand-rolled shadcn primitive is exported alongside a helper function from the same file (e.g. `parseBRL` + `CurrencyInput`), `react-refresh/only-export-components` warns. Move the helper to its semantic home — `parseBRL` belongs next to `formatBRL` in `lib/formatters/currency.ts`. Tests should import the helper from the formatter module, not from the component.
  - Native `<select>` styled with shadcn classes is enough for the PRICE/SAC binary choice (and any future enum selector). Don't add `@radix-ui/react-select` for a 2-option dropdown — the CLAUDE.md "no new deps without clear justification" rule applies. The styled native select supports keyboard navigation out of the box and integrates with RHF `register("system")` without any wrapping.
  - When changing `src/app/App.tsx` to consume a new provider, remember to update `src/app/App.test.tsx`'s render helper too (it doesn't inherit the main.tsx provider chain). Wrap with the new provider next to `ThemeProvider`. If you forget, every App-level test fails with "must be used within a *Provider".
---

## 2026-05-15 - US-011 — Extra payment card with reduce-term / reduce-installment tabs
- Added hand-rolled shadcn `Tabs` primitive at `src/components/ui/tabs.tsx` (`Tabs`/`TabsList`/`TabsTrigger`/`TabsContent`, controlled or uncontrolled, ARIA roles + `aria-selected`/`aria-controls`/`aria-labelledby` linking via `useId`-generated IDs, no `@radix-ui/react-tabs` dependency).
- Extended page-level state in `src/features/simulator/hooks/simulation-context.ts` and `simulation-provider.tsx`: `SimulationState` now carries `extraMonthly: number | null` plus a stable `setExtraMonthly` callback. The provider uses `useMemo`/`useCallback` so consumers don't churn on unrelated state updates.
- Built `src/features/simulator/components/extra-payment-card.tsx`. RHF + zodResolver(`prepaymentSchema`) on a single `extraMonthly` field validated `≥ 0` in pt-BR ("O pagamento extra não pode ser negativo"). Uses `useWatch` for reactive recompute and `useEffect(setExtraMonthly)` to publish the value to the simulation context whenever the form is valid (null on invalid). Heavy derivation (`useMemo`) builds `FinancingInputs` from the financing form values via a small `buildFinancingInputs` adapter, runs `generatePriceSchedule`/`generateSacSchedule` for the base, then `applyPrepaymentReduceTerm` and `applyPrepaymentReduceInstallment` for the two strategies. Renders two `<TabsTrigger>`s ("Reduzir prazo" / "Reduzir parcela") and shows five metrics per tab — `Economia em juros`, `Total pago`, `Novo prazo`, `Meses reduzidos`, `Diferença vs. base` — all formatted with `formatBRL` / `formatMonths`. Empty state copy "Informe um valor extra para simular" when extra is `0` or `null`; "Preencha os dados do financiamento para simular pagamentos extras" when the user typed an extra but no financing has been published yet.
- Replaced the `Pagamento extra` `PlaceholderCard` in `src/app/App.tsx` with `<ExtraPaymentCard />` (preserves `md:col-span-5` and the section title so existing App-shell tests still pass).
- Tests:
  - `src/components/ui/tabs.test.tsx` (5 cases): default-tab activation, click-to-switch, controlled mode driven by parent (`onValueChange` fires; clicks don't change visible state without re-render), `aria-controls`/`aria-labelledby` linking, throws when `<TabsTrigger>` is rendered outside `<Tabs>`.
  - `src/features/simulator/components/extra-payment-card.test.tsx` (9 cases): renders pt-BR label + section h3, empty-state copy when extra is null, empty-state copy when extra is exactly `0`, publishes extra value to `useSimulation` context, renders both tabs and all five metric labels with BRL strings once financing + extra are set, switches tabs and shows the other strategy's results, recomputes when extra changes (text differs), no tabs when extra is set but financing missing (different empty-state copy), pt-BR error on negative extra (and context stays `null`), SAC system support.
- Files changed: `src/components/ui/tabs.tsx` (new), `src/components/ui/tabs.test.tsx` (new), `src/features/simulator/components/extra-payment-card.tsx` (new), `src/features/simulator/components/extra-payment-card.test.tsx` (new), `src/features/simulator/hooks/simulation-context.ts`, `src/features/simulator/hooks/simulation-provider.tsx`, `src/app/App.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (237 tests across 18 files, 14 new cases for this story), `bun run build` ✅ (30s; bundle 364.79 kB JS gzip 114.46 / 35.07 kB CSS gzip 6.74).
- Browser verification: `bun run dev` boots cleanly on http://localhost:5173 (HTTP 200, valid HTML with `<title>Simulador Financeiro</title>` and the React `<div id="root"></div>` mount). All AC-required runtime behavior (CurrencyInput validation, reactive recompute, empty-state copy, tab switching, metric display) is covered by the jsdom unit suite. No headless browser tool available in this sandbox for an interactive screenshot — recommend a manual dev-browser pass before merging if visual regression matters.
- **Learnings for future iterations:**
  - Reactive context publish from a form: use `useWatch({ control })` to get the current values (without re-rendering the form on every keystroke), guard the publish with `isValid` from `formState`, and call `setX(null)` when invalid so downstream `useMemo`s don't try to operate on garbage. This is the simplest correct alternative to `handleSubmit` for forms whose results should update live (no Submit button). The `useEffect` deps array is `[derivedValue, isValid, setX]` — the context setter is `useCallback`-stable in our provider, so the effect doesn't loop.
  - The `Tabs` primitive is small enough to hand-roll (~120 LOC) and avoids dragging `@radix-ui/react-tabs` (~30 kB) into the bundle for what is functionally a styled `<button>` toggling a `hidden` panel. CLAUDE.md "no new deps without clear justification" wins here. Use `React.useId()` to generate the trigger/panel ID prefix so multiple `<Tabs>` on a page don't collide; don't fall back to a global counter.
  - When a `<TabsContent>` is inactive, render it with `hidden` instead of unmounting it. RTL `screen.getByRole("tabpanel", { name: ... })` only finds visible panels by default — passing `name` actually still resolves both panels; if you want to scope assertions to the active panel, query the `tabpanel` by its ARIA name (which matches the trigger's text) and check its content. Don't use `screen.queryAllByRole("tabpanel")` and index — that's brittle.
  - For tests that need the simulation context pre-seeded, write a tiny helper component (`<FinancingSeed financing={...} />`) that calls `setFinancing(financing)` from `useEffect` on mount. This is cleaner than wrapping in a custom provider and matches the "real" mounting order (provider state starts null, then a producer publishes). Pair with a `<XFingerprint />` consumer that renders `JSON.stringify(state)` into a `data-testid` for assertion.
  - The `prepaymentSchema.extraMonthly` field is **non-strict-positive** (`min(0)`), so RHF reports the form as `valid` when the user enters `0`. The component must distinguish "valid but no-op" (`extra === 0` → empty state) from "valid and runnable" (`extra > 0` → run engine). Don't lean only on `isValid` — also check the numeric value before computing tabs.
  - `formatMonths(months)` from `src/lib/formatters/number.ts` returns "X meses" for < 12 and "Y anos e Z meses" for ≥ 12 with proper singular handling. Use it for both `Novo prazo` (positive integer months) and `Meses reduzidos` — the helper handles `0` ("0 meses") naturally.
  - Adapter: `monthlyRate` in the form is **percent units** (e.g. `1` = 1%) but the engine expects a fraction. Always divide by `100` in the adapter (`new Decimal(values.monthlyRate).div(100)`). Forgetting this gives wildly wrong installments and was the gotcha to watch when wiring future cards (rent vs. buy, summary, charts).
---

## 2026-05-16 - US-012 — Rent vs. buy input card and results panel
- Added `src/features/simulator/components/rent-vs-buy-card.tsx`. RHF + zodResolver bound to `rentVsBuySchema`, pt-BR labels for all fields (Aluguel mensal, Reajuste anual do aluguel, Rendimento anual do investimento, Valorização anual do imóvel, Custos mensais de propriedade, Horizonte). Reuses `CurrencyInput`, `PercentageInput`, `Select`, `Card`, `Label` primitives. Form-level rate fields store percent units (e.g. `5` for 5%); the engine adapter `buildEngineInputs` divides by 100 to feed the engine.
- Derived "Rendimento mensal equivalente" rendered into an `<output>` element using `annualToMonthlyRate` from `core/finance/rent-vs-buy.ts` — formats as `formatPercentage(monthly*100, 4)`.
- Horizon input renders a unit selector (meses | anos) backed by local React state; the underlying `horizonMonths` field always stores months. Switching the unit toggles the displayed number (months ↔ months/12) and updates the form value via `setValue('horizonMonths', months, { shouldValidate: true })`.
- Custos mensais de propriedade is "optional": the form default is `0` instead of `null`, so omitting it doesn't block validity. Schema still validates ≥ 0.
- Results panel shows: best-scenario badge (`Comprar` emerald, `Alugar e investir` sky, `Empate` muted), `Patrimônio final (comprar)`, `Patrimônio final (alugar)`, `Diferença`, and `Ponto de equilíbrio` (formatMonths or `Não atinge ponto de equilíbrio`).
- Extended `simulation-context.ts` + `simulation-provider.tsx` with `rentVsBuy: RentVsBuyFormValues | null` + `setRentVsBuy`. The card publishes form values when `isValid`, clears to `null` otherwise — same pattern as `ExtraPaymentCard`.
- Replaced the rent-vs-buy placeholder in `src/app/App.tsx`.
- Updated `src/app/App.test.tsx` placeholder count from `>= 5` to `>= 4` to match the reduced number of remaining placeholder cards.
- Files changed: `src/features/simulator/components/rent-vs-buy-card.tsx`, `src/features/simulator/components/rent-vs-buy-card.test.tsx`, `src/features/simulator/hooks/simulation-context.ts`, `src/features/simulator/hooks/simulation-provider.tsx`, `src/app/App.tsx`, `src/app/App.test.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (247 tests across 19 files, 10 new RentVsBuyCard cases), `bun run build` ✅.
- Manual browser verification still pending (no MCP browser tools available in this iteration).
- **Learnings for future iterations:**
  - Theme palette is all reds/oranges — there's no `--success` / `--info` / green / blue in the tweakcn `index.css`. For semantic badges (this scenario badge being the first), prefer plain Tailwind utility colors (`emerald-*`, `sky-*`) with dark-mode variants (`dark:text-emerald-400`) rather than inventing fake theme tokens. The "no hardcoded colors" rule in `CLAUDE.md` is scoped to **card surfaces** (`bg-card`/`text-card-foreground`/`border-border`), not to every element.
  - Convention for "optional" form fields with a `min(0)` schema rule: default the field to `0` in `defaultValues`, not `null`. The `CurrencyInput` returns `null` when blank, which would fail the `required_error` rule on the schema. Setting `0` as default keeps the form valid while still surfacing a non-negative error if the user types a negative.
  - When a form publishes state to context based on `isValid`, write a second `waitFor` for the state assertion. The error message renders synchronously on `mode: "onChange"` (Zod is sync), but the `useEffect` that publishes the state only runs after the next commit — so a single `waitFor` for the error can race the state update. Same pattern as how `ExtraPaymentCard` did it.
  - Test brittleness on `data-scenario`: I initially tried asserting that `bestScenario` flips when an input changes, but with the default fixture (rent 2500 / return 10% / financing 0,8%) the scenario can stay `buy` even after a 4× rent bump. Asserting that the rendered "Diferença" value changes is sturdier and still proves reactivity.
  - Unit-switcher pattern for "months/years" input: store the canonical value (months) in the form, render a derived display value (`months` or `months/12`), and re-validate via `setValue(name, months, { shouldValidate: true })` after toggling. Don't store the unit in the form schema — keep it as local React state. This keeps the schema engine-agnostic and reusable.
  - The `useEffect` that publishes form state to context can use spread-watched values in deps, BUT the deps must be the *individual fields*, not `watched` itself — `useWatch` returns a fresh object each render so a dep on `watched` would fire every render. Mirror the `ExtraPaymentCard` pattern: list each `watched.fieldName` explicitly.
  - When an existing test counts placeholder empty-state cards (e.g. `App.test.tsx`), each user story that "Replaces the X placeholder in App.tsx" must also bump that count down by one. Easy to miss because the test isn't named after the feature being added.
---

## 2026-05-16 - US-013 — Results summary card
- Created `src/features/simulator/components/results-summary-card.tsx` rendering all 9 AC-required metrics (Valor financiado, Parcela inicial, Total pago base/com extra, Total de juros base/com extra, Economia total, Prazo original, Novo prazo, Meses reduzidos). Each value uses the pt-BR formatters from US-002 (`formatBRL`, `formatMonths`) and every monetary cell carries the `.font-tabular` utility from US-008.
- Card reads `financing`, `extraMonthly`, and the new `extraStrategy` from `useSimulation()` and computes its own derivations in a single `useMemo` keyed on those three values, so it updates reactively as any input changes. Engine logic stays in `core/finance` (`generatePriceSchedule` / `generateSacSchedule` + `summarizeSchedule` for the base, `applyPrepaymentReduceTerm` / `applyPrepaymentReduceInstallment` for the with-extra columns).
- When extra is `null` or `≤ 0`, with-extra values mirror the base, `Economia total` is `R$ 0,00`, `Meses reduzidos` is `0 meses`, and `Novo prazo` equals `Prazo original` — so the card never shows blank fields once financing is set. Empty state ("Preencha os dados para simular") only shows when no valid financing has been submitted.
- Added `extraStrategy: "term" | "installment"` (+ `setExtraStrategy`) to the simulation context so the summary card knows which prepayment column to surface. Wired `ExtraPaymentCard`'s `<Tabs>` into the context (controlled mode via `value`/`onValueChange`) so switching tabs is the source-of-truth for "estratégia selecionada".
- `Economia total` uses a `DeltaMetric` helper: positive → `text-emerald-600 dark:text-emerald-400`, negative → `text-destructive`, zero → `text-foreground`. The `data-delta-sign` attribute (positive/negative/zero) makes the colorization easy to assert in tests without coupling to literal class names.
- Replaced the "Resumo dos resultados" placeholder in `App.tsx` with `<ResultsSummaryCard />`. The card keeps `md:col-span-12` so the grid layout is unchanged. Section title stays "Resumo dos resultados" so the App-shell ordering test still matches.
- Tests: added `results-summary-card.test.tsx` with 10 cases covering empty state, all 9 metrics rendering, the PRICE+SAC initial-installment golden values (R$ 3.392,64 PRICE / R$ 4.311,11 SAC for 400k @ 0,8% × 360), the no-extra parity invariant, positive-delta coloring, strategy switching (term → installment changes both the with-extra total and meses reduzidos), and that the `.font-tabular` utility lands on the `<dd>` value nodes.
- Files changed: `src/features/simulator/hooks/simulation-context.ts`, `src/features/simulator/hooks/simulation-provider.tsx`, `src/features/simulator/components/extra-payment-card.tsx`, `src/features/simulator/components/results-summary-card.tsx`, `src/features/simulator/components/results-summary-card.test.tsx`, `src/app/App.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (257 tests across 20 files, 10 new), `bun run build` ✅.
- Manual browser verification still pending — no MCP browser tools available in this iteration; the runtime behavior is fully covered by the jsdom suite (empty state, metric rendering, BRL formatting, delta coloring, strategy reactivity).
- **Learnings for future iterations:**
  - Per-row label inclusion in `textContent`: when a metric is rendered as `<div><dt>Label</dt><dd>R$ 1.234,56</dd></div>`, the outer `textContent` is `"LabelR$ 1.234,56"`. Two metrics with the same monetary value but different labels will produce different `textContent` strings. For "equal-value" assertions across metrics, query the `<dd>` (`.querySelector("dd")?.textContent`) instead of comparing the parent's `textContent`. Same pitfall would hit US-014 (table cells with surrounding `<th>` headers).
  - When the AC says "in success color"/"in destructive color" but the theme has no `--success` token, prefer literal Tailwind utilities with dark variants (`text-emerald-600 dark:text-emerald-400`, `text-destructive`) and expose a `data-delta-sign` attribute on the outer element. Lets tests assert sign without coupling to exact class names, and keeps the visual decision in one place if the palette ever gains a `--success` token.
  - The `<Tabs>` primitive at `src/components/ui/tabs.tsx` supports controlled mode via `value` + `onValueChange`. Use this when two components need to share the active tab (here: ExtraPaymentCard owns the tabs UI, ResultsSummaryCard observes the active strategy via context). Don't fork the primitive — `controlled === undefined ? internal : controlled` is already in place.
  - Adding a new field to `SimulationContextValue` requires updating: the type in `simulation-context.ts`, the `useState`/`useCallback`/`useMemo` deps in `simulation-provider.tsx`, and any test seed components that use `useSimulation()`. Forget the provider's `useMemo` deps and the new field reads as stale on rerenders.
  - When computing PRICE installment golden fixtures for tests, run the engine once via `bun -e "..."` rather than trusting mental log/exp math — for 400k @ 0,8% × 360 I initially guessed R$ 3.241,32 but the actual rounded value is R$ 3.392,64 (`(1.008)^360 ≈ 17.59`, so installment ≈ P·i·(1+i)^n / ((1+i)^n − 1) ≈ 3.392,64). Same heuristic as US-003/US-004 fixture generation.
  - When a feature card consumes both inputs (financing/extraMonthly/extraStrategy from context) AND does its own derivation, keep the derivation in a single `useMemo` keyed on the relevant context fields. Doing N separate `useMemo`s for the 9 metrics would recompute the schedule N times per render. One memo computes both the base summary and the (optional) prepayment result, then the JSX just reads from the figures object.
---

## 2026-05-16 - US-014 — Amortization data table
- Added `src/features/simulator/components/amortization-table.tsx`. A self-contained `<Card>` that consumes `financing` + `extraMonthly` from `useSimulation()` and renders a 10-column data table: Mês, Parcela base, Juros, Amortização, Pagamento extra, Pagamento total, Saldo devedor, Juros acumulados, Amortização acumulada, Status. The scenario selector (shadcn `<Select>`) flips between three datasets — Base, Com extra (reduzir prazo), Com extra (reduzir parcela) — driven by `rowsForScenario()` which calls `generatePriceSchedule` / `generateSacSchedule` for base and `applyPrepaymentReduceTerm` / `applyPrepaymentReduceInstallment` for the with-extra cases. No financial logic added in the component itself; row data flows straight from `core/finance`.
- A small `decorateBase()` / `decoratePrepayment()` pair turns the raw `ScheduleRow[]` / `PrepaymentScheduleRow[]` into the unified `TableRow[]` shape used by the table (and that US-019's Excel export will reuse): unified columns plus running `cumulativeInterest` / `cumulativeAmortization` and a `status` of `"Última parcela"` on the final row.
- Toolbar (role=`toolbar`) renders: scenario `<Select>`, month-range filter (two `<Input type="number">` for Mês inicial / Mês final + a "Limpar filtro" ghost button when either is filled), and a Colunas `<details>` dropdown with one checkbox per column. The `<details>` element gives a fully accessible "menu of toggles" without pulling in a popover dependency.
- Table is sortable on every numeric column via a header `<button>` with `aria-sort="ascending|descending|none"`. Click cycles asc → desc → unsorted. Sort comparator converts `Decimal` to `number` via `.toNumber()`; that's fine for ordering monetary values up to ~15 digits and avoids implementing a per-row `Decimal.cmp` chain.
- Pagination: 12 rows per page, `Anterior` / `Próxima` buttons + "Página X de Y · N parcela(s)" indicator. A `useEffect` resets `page = 0` whenever scenario / filter / dataset changes so the user can never sit on an out-of-range page.
- Monetary cells use `font-tabular text-right`; the Status cell is left-aligned non-tabular. Outer scroll wrapper uses `overflow-x-auto` and the table sets `min-w-[960px]` so all 10 columns fit horizontally on small screens with a horizontal scroll fallback.
- Wired into `src/app/App.tsx`: replaced the `Tabela de amortização` placeholder with `<AmortizationTable />` (keeps `md:col-span-12` so layout is unchanged). Bumped the `App.test.tsx` placeholder-count assertion from `>= 4` to `>= 3`.
- Tests (`amortization-table.test.tsx`, 11 cases): empty state when financing is null; section title + scenario options + all 10 column headers; pagination (12/page, 30 pages for the default 360-month fixture, Próxima/Anterior advance/retreat); month-range filter (5..9 → 5 rows, resets page); sorting (Saldo devedor asc puts month 360 first, second click flips to desc); column visibility (uncheck "Juros" removes the header); scenario switch (Base → "Com extra (reduzir prazo)" with extraMonthly=1500 changes the extraPayment cell from R$ 0,00 to R$ 1.500,00); final row Status = "Última parcela"; `.font-tabular` lands on monetary cells but not on Status; engine-derived PRICE installment matches R$ 4.114,45 for 400k @ 1% × 360; outer scroll container has `overflow-x-auto`.
- Files changed: `src/features/simulator/components/amortization-table.tsx` (new), `src/features/simulator/components/amortization-table.test.tsx` (new), `src/app/App.tsx`, `src/app/App.test.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality checks: `bun run typecheck` ✅, `bun run lint` ✅, `bun run test` ✅ (268 tests across 21 files, 11 new), `bun run build` ✅ (40s; bundle 389.86 kB JS gzip 120.23 / 38.00 kB CSS gzip 7.31 — modest growth from the new table component).
- Browser verification: `bun run dev` boots cleanly (port 5173 was already held by something so Vite fell back to 5174; HTTP 200 returned). Runtime behavior (scenario switch, sort, filter, pagination, column visibility, status label, monetary cell typography) is fully exercised by the jsdom suite. No MCP browser tool available in this sandbox for an interactive light/dark screenshot — recommend a manual dev-browser pass before merging if visual regression matters.
- **Learnings for future iterations:**
  - The PRD says "uses DiceUI Radix Data Table" but DiceUI's `bunx shadcn@latest add ...` registry requires network access (https://diceui.com) that the sandbox blocks. Hand-rolling a small custom table (sort + paginate + filter + column-visibility, ~300 LOC) costs less than dragging in `@tanstack/react-table` (~25 kB) for the only table in the app. Same trade-off as the hand-rolled Tabs primitive in US-011. Document the deviation in the progress entry so reviewers know it was intentional.
  - `<details><summary>` is the cheapest possible "popover menu" for toolbar actions like column visibility: native, keyboard-accessible, no portal, no outside-click handling needed. Position the panel with `absolute z-10 mt-1` inside `<details>` so it floats over the table; the panel only renders when `<details>` is open (browser-driven), so RTL queries against its contents inside a test work without additional setup (jsdom's `details/summary` support is fine for `screen.getByRole("checkbox", ...)`).
  - For sorting `Decimal` values, `Decimal.toNumber()` is fine — sorting only needs ordering, and the precision loss past ~15 digits doesn't affect monetary row ordering. Don't implement a per-row `Decimal.cmp` chain unless the values you're sorting genuinely overflow `Number.MAX_SAFE_INTEGER`.
  - The 4-stage derivation chain (`allRows → filteredRows → sortedRows → pageRows`) needs a `useEffect` that resets `page` to 0 when any upstream dep (scenario, filter, dataset) changes. Without that reset, the user can be sitting on page 25 of base, filter down to "month 5-9", and see an empty "Página 25 de 1" page. Capping the rendered page with `safePage = Math.min(page, pageCount - 1)` is a belt-and-suspenders fallback so the table never tries to slice past the array end.
  - Test fixtures for the PRICE engine: run `bun -e "import('./src/core/finance/...').then(...)"` for golden values; my mental math gave R$ 4.114,42 but the actual rounded installment for 400k @ 1% × 360 is R$ 4.114,45. Same lesson as US-003/US-004/US-013 — never trust hand math past 3 decimal places, let the engine compute the fixture.
  - When a user story "Replaces the X placeholder in App.tsx", drop the empty-state counter in `App.test.tsx` by exactly one. Each iteration that forgets this gets a confusing test failure on an unrelated test. Codebase pattern #53 already covers this; the muscle memory is sticking.
  - Status column convention for amortization rows: `"Em andamento"` for normal rows, `"Última parcela"` for the final row (the row that drives the balance to zero). Future Excel export (US-019) can reuse the same status string verbatim — keep the values short and pt-BR.
  - Row decoration (`decorateBase` / `decoratePrepayment`) is the seam where US-019 (Excel export) will plug in. The engine returns bare `ScheduleRow[]` / `PrepaymentScheduleRow[]`; the table-shaped `TableRow[]` (with cumulative columns + status) lives in `amortization-table.tsx`. When wiring the export, lift these helpers into a shared module (e.g. `src/features/simulator/lib/decorate-schedule.ts`) so both consumers share one definition.
---
