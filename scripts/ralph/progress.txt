# Ralph Progress Log
Started: Mon May 18 19:39:03 PDT 2026

## Codebase Patterns
- **Worker vs SPA TypeScript split**: `src/server/` is excluded from `tsconfig.app.json` and typechecked via `tsconfig.server.json` with `types: ["@cloudflare/workers-types"]`. This keeps DOM lib out of the worker compile and avoids conflicting global types. Add new server code under `src/server/` and it gets picked up automatically.
- **Worker bindings live in `src/server/env.d.ts`** (`interface Env`). Hono routes type the env via `new Hono<{ Bindings: Env }>()`. Add every new binding/secret here _and_ wire it into `wrangler.jsonc` (`vars` for plain values, `wrangler secret put` for secrets, `d1_databases` for D1).
- **Build output structure under cloudflare/vite-plugin**: `bun run build` writes the SPA to `dist/client/` and the bundled worker + generated wrangler config to `dist/calculadora_financeira/`. The plugin rewrites paths so `wrangler deploy` uses the redirected config — never point wrangler at the source `wrangler.jsonc` directly.
- **Cloudflare custom_domain routes do NOT accept wildcards or paths.** Use `{ pattern: "calculadorafinanceira.app", custom_domain: true }`, not `calculadorafinanceira.app/*`.
- **Node 20 in the sandbox can't run wrangler directly** (needs v22). Always invoke via `bunx --bun wrangler ...` so the bun-bundled runtime is used.
- The `migrations/` directory must exist (even if empty with a `.gitkeep`) because `wrangler.jsonc` references `migrations_dir: "migrations"`.
- **Better Auth on D1**: `betterAuth({ database: env.DB })` is enough — the kysely-adapter auto-detects D1 via `batch/exec/prepare` and dynamically loads `D1SqliteDialect`. No `{ provider: 'd1' }` wrapper needed in v1.6.x. Build a fresh `createAuth(env)` per Hono request (no module-level singletons in Workers — each request has its own bindings).
- **Shared auth options pattern**: keep plugin/session/email options in `src/server/auth-options.ts` (no env deps) and re-use them in both `src/server/auth.ts` (Worker, `database: env.DB`) and `auth.config.ts` (CLI, in-memory SQLite). Prevents the CLI-generated schema from drifting from the runtime config.
- **`@better-auth/cli generate` needs a real Kysely-compatible DB** because it calls `db.introspection.getTables()`. Bun runs the CLI through jiti/Node, so `bun:sqlite` is unavailable there; the fallback is to install `better-sqlite3` (native build) or hand-write the SQL using `@better-auth/core/db/get-tables` as the source of truth. The hand-written `0001_better_auth.sql` mirrors the Kysely SQLite output exactly: lowercase DDL keywords, double-quoted identifiers, `text`/`integer`/`date` types, `references "user" ("id") on delete cascade`, indexes appended as `create index "<table>_<col>_idx"`.
- **Hono middleware registration order matters**: `app.use(path, mw)` only applies to handlers registered AFTER the `use` call. Put public routes first, then `app.use(requireUser)`, then protected routes. The `/api/*` 404 fallback can sit after the middleware safely (returns 401 for unauthed unknown routes — fine).
- **Server-side vitest pattern**: tests under `src/server/**` are picked up by `tsconfig.server.json` (workers-types only, no DOM lib). Import everything explicitly from `vitest` (no globals), mock dependencies with `vi.mock("../auth", () => ({...}))` at module top, and use Hono's `app.request(path, init, env)` to drive the middleware with a fake `Env`. Hono serializes `Date` to ISO strings when round-tripping through `c.json`, so assertions must compare `.toISOString()`.
- **Domain migrations use snake_case identifiers**, distinct from Better Auth's camelCase. Money is stored as integer cents, interest rates as basis points (1 bp = 0.01 %), and dates as ISO-8601 strings — this keeps Decimal.js reconstructions lossless. Cross-namespace FKs are fine (`financing_scenarios.user_id REFERENCES "user"("id")`), since D1 enables foreign keys by default.
- **Running migrations in sandbox**: the `db:migrate:local` script calls `wrangler` directly, which fails under Node 20. Always run via `bunx --bun wrangler d1 migrations apply <db> --local` (or `--remote`) so wrangler executes under Bun's bundled v22-compatible runtime. CI on Node 22 (US-038/039) can use the npm scripts directly.
- **Env-aware Better Auth options live in `createAuth(env)`, not `sharedAuthOptions`.** The CLI config uses `sharedAuthOptions` directly with an in-memory SQLite, and it can't see runtime bindings like `env.RESEND_API_KEY` or `env.EMAIL_FROM`. Anything that needs those — `emailVerification.sendVerificationEmail`, `emailAndPassword.sendResetPassword`, future Turnstile hooks — gets built inside `createAuth(env)` by spreading the shared block and adding the env-aware bits. Keeps the CLI generation deterministic and the runtime config single-source.
- **Mocking SDK modules in vitest**: declare a `vi.fn()` (e.g. `const sendMock = vi.fn()`) at module scope and reference it from inside the `vi.mock(...)` factory. Then `sendMock.mockReset()` in `beforeEach` and `sendMock.mockResolvedValueOnce(...)` per test. This is cleaner than reaching into the constructor's mocked implementation. Works because `vi.mock` is hoisted but the captured-by-reference variable is still patched by the time the factory runs.
- **`globalThis.fetch` spies must be (re)created INSIDE `beforeEach`**, not captured once at the top of a test. `vi.restoreAllMocks()` (in either `beforeEach` or `afterEach`) detaches the previous spy from the property; any reference held in a closure becomes a dangling spy that never observes calls. Pattern: `let fetchSpy: ReturnType<typeof vi.spyOn<typeof globalThis, "fetch">>;` at module scope, then `beforeEach(() => { vi.restoreAllMocks(); fetchSpy = vi.spyOn(globalThis, "fetch"); })`. Queue per-test responses via `fetchSpy.mockResolvedValueOnce(new Response(...))`.
- **Better Auth global hooks**: top-level `hooks: { before, after }` accept a single `AuthMiddleware` (built with `createAuthMiddleware` from `better-auth/api`). The before-hook fires for every endpoint after the router parses the body but BEFORE the endpoint's zod schema runs — so `ctx.body` carries the raw parsed body (unknown fields intact). This matters for /sign-in/email, whose `signInEmailBodySchema = z.object({...})` strips unknowns; only /sign-up/email uses `.and(z.record(z.string(), z.any()))` and would keep them at endpoint time. Match endpoints via `ctx.path === "/sign-up/email"` etc. (paths are relative to `/api/auth/`, not absolute). Throw `new APIError("BAD_REQUEST", { message, code })` from `better-auth/api` to fail the request as a 400 JSON.
- **Better Auth client on the frontend** is the same `better-auth` package, imported from `better-auth/react` (the `./react` subpath export). `createAuthClient({ baseURL })` returns an object whose `useSession()` is a React hook returning `{ data, isPending, isRefetching, error, refetch }`. Derive `AuthUser` / `AuthSession` via `ReturnType<typeof authClient.useSession>['data']` instead of reaching for internals — the React client doesn't expose `$Infer`, only the vanilla one does.
- **Mocking the authClient in jsdom tests**: instantiating the real `createAuthClient` in jsdom pulls in nanostores' session refresh, focus/online managers, and `BroadcastChannel` — heavy and flaky. Pattern that avoids it: declare `const useSessionMock = vi.fn()` at module scope and `vi.mock("./auth-client", () => ({ authClient: { useSession: () => useSessionMock() } }))`. Each test calls `useSessionMock.mockReturnValue({ data, isPending, ... })`. `renderHook` from `@testing-library/react` then exercises `useCurrentUser()` cleanly.
- **Router factoring**: `App.tsx` does ONLY `<BrowserRouter><AppRoutes/></BrowserRouter>`; the route table lives in `src/app/routes.tsx` exporting `<AppRoutes/>`. Tests use `<MemoryRouter initialEntries={[path]}><AppRoutes/></MemoryRouter>` — wrapping `<App/>` instead would nest two routers. Layouts (`AppShell`, `AuthLayout`) and gates (`ProtectedRoute`) are layout routes that render `<Outlet/>`: `<Route element={<AppShell/>}><Route path="/foo" .../></Route>`. ProtectedRoute also a layout route — wrap a Route group with `<Route element={<ProtectedRoute/>}>...</Route>` so the gate runs once and all children inherit. Page components live under `src/features/<area>/pages/<route>-page.tsx`; the `app/` dir holds router-only chrome (shells, gates, 404).
- **shadcn `Form` component** lives at `src/components/ui/form.tsx` (hand-written because `bunx shadcn add form` failed offline). Uses our existing `Label` (not `@radix-ui/react-label`) and `@radix-ui/react-slot` for `FormControl`. Standard usage: `<Form {...form}><form onSubmit={form.handleSubmit(...)}><FormField control={form.control} name="x" render={({field}) => <FormItem><FormLabel>…</FormLabel><FormControl><Input {...field}/></FormControl><FormMessage/></FormItem>}/></form></Form>`. `FormMessage` auto-renders the zod error for the field — don't render error text manually.
- **Sending extra fields to typed Better Auth endpoints**: `authClient.signUp.email` / `signIn.email` have STRICT TS types (`InferSignUpEmailCtx` in `better-auth/dist/client/path-to-object.d.mts`) that don't accept unknown keys. To smuggle extras (e.g. `turnstileToken`) past the type check, put them in `fetchOptions: { body: { … } }` — the client proxy merges that into the POST body before sending, and the Worker's before-hook still reads them off `ctx.body`. Confirmed working in `src/features/auth/pages/register-page.tsx`.
- **Turnstile dev bypass on the frontend**: `<TurnstileField onToken={…}/>` (in `src/features/auth/components/`) skips the real widget when `VITE_TURNSTILE_SITE_KEY` is empty and fires `onToken("dev-token-no-site-key")` immediately. The Worker's `verifyTurnstile` bypasses on the same condition (empty `TURNSTILE_SECRET_KEY` → returns `true`). Keep these in sync: changing one without the other breaks dev mode.
- **Testing auth pages in jsdom**: mock `@/lib/auth-client` with a module-scope `vi.fn()` to stub the endpoint(s) you care about; mock `@/features/auth/components/turnstile-field` with a button stub that calls `onToken` synchronously. Together these keep tests fast and deterministic — no real Turnstile script load, no real `createAuthClient` nanostores boot, no network. Wrap with `<MemoryRouter initialEntries={[path]}>` + `<Routes>` containing the source page + simple `<div>` placeholders for destinations so post-submit navigation can be asserted via `screen.findByText(...)`.
- **shadcn `dashboard-01` is offline-blocked**: both `bunx shadcn@latest add dashboard-01` and direct fetches of `ui.shadcn.com/r/...` fail in this sandbox (no network to that host). Block components are hand-written from the upstream sources. The shell pieces live at `src/components/{app-sidebar,site-header,nav-main,nav-user,nav-documents}.tsx` and use `react-router-dom` (`NavLink`, `Link`, `useNavigate`) instead of `next/link` + `usePathname`. `section-cards`, `chart-area-interactive`, `data-table` are intentionally stubbed — the real charts/tables for this app live under `src/features/simulator/components/*` and the dashboard-01 demos won't be wired up.
- **Sidebar primitive split-vs-monolith**: `src/components/ui/sidebar.tsx` carries the full upstream primitive (provider + Sheet-backed mobile drawer + cookie persistence + `Ctrl/Cmd+B` shortcut + 20+ subcomponents). Sub-components (`SidebarMenuButton`, `SidebarMenuSubButton`, etc.) accept `asChild` and route through `@radix-ui/react-slot`. Pair `<NavLink to=...>` with `<SidebarMenuButton asChild isActive={isActive}>` to get active-route highlighting that matches React Router state. `useIsMobile()` (in `src/lib/use-mobile.ts`, 768 px breakpoint) drives the drawer-vs-rail choice automatically.
- **CSS sidebar tokens were already defined in `src/index.css` from earlier work** (`--sidebar`, `--sidebar-foreground`, `--sidebar-primary`, `--sidebar-accent`, `--sidebar-border`, `--sidebar-ring` for both light + dark), and the `@theme inline` block maps them to `--color-sidebar-*` so Tailwind utilities like `bg-sidebar`, `text-sidebar-foreground` work. The dashboard-01 shell drops in with zero CSS-token additions. If a future block expects extra tokens (e.g. `--chart-area-*`), audit `@theme inline` first.

---

## 2026-05-18 - US-001
- Implemented Cloudflare Workers + Hono + D1 binding scaffold.
- Files changed:
  - `wrangler.jsonc` (new): worker config with assets binding `ASSETS`, D1 binding `DB`, vars EMAIL_FROM + ENVIRONMENT, observability enabled, custom_domain route `calculadorafinanceira.app`, compat date 2026-05-01 + nodejs_compat.
  - `src/server/index.ts` (new): Hono `Hono<{ Bindings: Env }>` app with `GET /api/health` returning `{ ok, environment }`, a 404 JSON for unmatched `/api/*`, and SPA fallback via `c.env.ASSETS.fetch` (re-fetching `/index.html` on 404).
  - `src/server/env.d.ts` (new): `Env` interface with `DB: D1Database`, `ASSETS: Fetcher`, `EMAIL_FROM`, optional `ENVIRONMENT`.
  - `vite.config.ts`: added `cloudflare({ configPath: "./wrangler.jsonc" })` plugin.
  - `package.json`: added scripts `dev:worker`, `deploy`, `deploy:dry-run`; new deps `hono`, `@cloudflare/workers-types`, `@cloudflare/vite-plugin`, `wrangler`.
  - `tsconfig.server.json` (new) + `tsconfig.json` reference: dedicated server-side typecheck with workers-types only.
  - `tsconfig.app.json`: excluded `src/server` so worker code isn't double-checked under DOM lib.
  - `migrations/.gitkeep` (new): keeps the empty dir so wrangler doesn't warn.
  - `.gitignore`: added `.wrangler/` and `.dev.vars`.
- Verified: `bun run typecheck`, `bun run lint`, `bun run test` (352 passed), `bun run build`, `bunx --bun wrangler deploy --dry-run` (Total Upload 58 KiB, bindings DB/ASSETS/EMAIL_FROM/ENVIRONMENT listed).
- **Learnings for future iterations:**
  - When wrangler complains about Node 20, use `bunx --bun wrangler ...` instead of direct `wrangler`. The script `deploy:dry-run` invokes wrangler directly, so it fails locally with Node 20; CI will be fine on Node 22 (US-038).
  - The `cloudflare` plugin moves the SPA from `dist/` to `dist/client/`. If any existing tooling assumed `dist/index.html` is the SPA index, it now needs updating to `dist/client/index.html` (currently no code depends on this).
  - `custom_domain` routes cannot contain `/*` or path segments — Cloudflare validates at deploy time. PRD wording was technically invalid; corrected in implementation and noted in PRD `notes`.
  - `assets` + `routes` work together: the asset binding handles static files and the SPA fallback (`not_found_handling: "single-page-application"`); the worker handles `/api/*` and any explicit route handler that returns before falling through.
---

## 2026-05-18 - US-002
- Mounted Better Auth on the Worker with D1 storage and added the initial migration.
- Files changed:
  - `src/server/auth.ts` (new): `createAuth(env: Env)` factory wrapping `betterAuth({ ...sharedAuthOptions, database: env.DB, secret: env.BETTER_AUTH_SECRET, baseURL: env.BETTER_AUTH_URL })`. Exports `Auth` type.
  - `src/server/auth-options.ts` (new): `sharedAuthOptions` (emailAndPassword w/ requireEmailVerification + 1h reset token TTL, 30-day session w/ 1-day rolling update, trustedOrigins for localhost:5173 + calculadorafinanceira.app). Reused by both runtime and CLI configs.
  - `src/server/index.ts`: added `app.on(["POST","GET"], "/api/auth/*", (c) => createAuth(c.env).handler(c.req.raw))` before the `/api/*` 404 fallback.
  - `src/server/env.d.ts`: added `BETTER_AUTH_SECRET: string` and `BETTER_AUTH_URL: string` to the `Env` interface.
  - `auth.config.ts` (new, project root): CLI-only Better Auth instance using `new Database(":memory:")` from `bun:sqlite`. Tagged with `// @ts-ignore` because the file isn't part of any tsconfig include (Bun runs it directly).
  - `migrations/0001_better_auth.sql` (new): hand-written DDL for `user`, `session`, `account`, `verification` + indexes (`session_userId_idx`, `account_userId_idx`, `verification_identifier_idx`). Matches Kysely SQLite's `compile().sql` output for the schema returned by `@better-auth/core`'s `getAuthTables`.
  - `docs/schema.md` (new): table/column reference for every D1 migration. Section for `0001_better_auth.sql` with field types and FKs; ends with a "Regenerating" note explaining the bun-sqlite/native-binding gotcha.
  - `package.json`: added `better-auth` dep + `@better-auth/cli` devDep; new scripts `db:generate`, `db:migrate:local`, `db:migrate:remote`.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (352/352) ✓, `bun run build` ✓, `bunx --bun wrangler deploy --dry-run` ✓ (bundled output now 1505 KiB; pulled in `d1-sqlite-dialect`, `bun-sqlite-dialect`, `node-sqlite-dialect` as lazy chunks — expected from better-auth's kysely adapter).
- **Learnings for future iterations:**
  - The kysely-adapter's D1 path is detected purely by duck-typing the binding (`"batch" in db && "exec" in db && "prepare" in db`). Passing `env.DB` directly works in v1.6.x; the PRD's `{ provider: 'd1', binding: env.DB }` syntax is not supported and will type-error. Update later PRD entries that still use that form (e.g. US-004's surrounding context).
  - `bun add @better-auth/cli` triggers a peer install of `better-sqlite3`, which requires `node-gyp` and a CXX toolchain. Use `--ignore-scripts` to install the CLI in sandboxes without native build deps; the CLI's `generate` command still won't work without a real SQLite (bun-sqlite OR better-sqlite3 native), but the package install succeeds.
  - When hand-writing migrations to match the better-auth schema, use `@better-auth/core/dist/db/get-tables.mjs` as the source of truth. Each field has `type` (string|boolean|number|date|json), `unique`, `required`, `references`, `index`, and a `fieldName` (camelCase). Boolean → `integer`, date → `date`, string|references → `text`. Indexes only emit for fields with `index: true` (and inherit `unique` if both are set).
  - `@better-auth/cli/dist/generators-*.mjs::generateKyselySchema` joins compiled statements with `;\n\n` and ends the file with `;`. My hand-written SQL uses one statement per `CREATE TABLE`/`CREATE INDEX` separated by blank lines — wrangler's SQL parser handles both forms.
  - The Worker bundle pulls in three SQLite dialects (D1, Bun, Node) because the kysely-adapter `await import()`s them conditionally. That's fine on Workers (dead-code at runtime), but bundle size jumps ~1.4 MiB. Watch this if we get near the 10 MiB free-tier limit.
  - `useDefineForClassFields` + `noUncheckedSideEffectImports` in tsconfig.app.json conflict with raw side-effect imports; keep server-only code in `src/server/` so it lands in `tsconfig.server.json` (which doesn't have the DOM lib and so won't reject worker types).
  - The custom auth.config.ts at the repo root is NOT included in any tsconfig (intentional — Bun runs it directly). If you ever need to typecheck it, add a fourth project ref; for now `@ts-ignore` on the `bun:sqlite` import is sufficient.
---

## 2026-05-18 - US-003
- Implemented `requireUser` Hono middleware and wired it into the Worker app.
- Files changed:
  - `src/server/middleware/require-user.ts` (new): exports `requireUser`, `AuthVariables`, `AuthUser`, `AuthSession`. Uses `createMiddleware<{ Bindings: Env; Variables: AuthVariables }>` from `hono/factory`. Calls `createAuth(c.env).api.getSession({ headers: c.req.raw.headers })` inside try/catch; null/throw → 401 JSON `{ error: 'unauthorized' }`; success → `c.set('user', ...)` + `c.set('session', ...)`.
  - `src/server/middleware/require-user.test.ts` (new): 4 vitest cases — valid session passes and exposes user/session, missing session → 401, expired session → 401, getSession throw → 401. Mocks `../auth` via `vi.mock`. Drives Hono via `app.request(path, init, env)` with a fake `Env`.
  - `src/server/index.ts`: typed root app as `Hono<{ Bindings: Env; Variables: AuthVariables }>`. Mounted `app.use("/api/*", requireUser)` AFTER `/api/health` + `/api/auth/*` and BEFORE the `/api/*` 404 fallback so the public routes stay public and any future protected routes register below the middleware.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (356/356, +4 new) ✓, `bun run build` ✓, `bunx --bun wrangler deploy --dry-run` ✓ (worker bundle still 1505 KiB — no growth).
- **Learnings for future iterations:**
  - Hono's `createMiddleware<{ Bindings; Variables }>` is the cleanest way to type a middleware that needs both env bindings and shared context variables. The Variables type is the canonical place to declare what `c.get(...)` returns inside route handlers downstream.
  - Better Auth's `auth.api.getSession` returns `null` for missing/expired/revoked sessions (no thrown error in the happy "no session" path). Wrap it in try/catch anyway — D1/network failures during the lookup should not 500; they should fail closed as 401.
  - Hono's route dispatcher composes ALL matched handlers/middlewares in registration order. A `use()` registered AFTER a `get()` does NOT wrap the earlier handler. This makes `/api/health` and `/api/auth/*` trivially "public" — just register them before `app.use(requireUser)`.
  - Mock Better Auth in tests by `vi.mock("../auth", () => ({ createAuth: vi.fn() }))`. The factory must return something whose `.api.getSession` is a function — Hono will only ever invoke that path. Cast through `as unknown as ReturnType<typeof createAuth>` to satisfy TS without pulling the full Better Auth surface.
---

## 2026-05-18 - US-004
- Added the domain migration that creates the two tables underpinning the History feature.
- Files changed:
  - `migrations/0002_domain.sql` (new): creates `financing_scenarios` (id, user_id FK → user(id) ON DELETE CASCADE, name, property_value_cents, down_payment_cents, term_months, annual_rate_basis_points, start_date, created_at, archived_at) and `payment_history` (id, scenario_id FK → financing_scenarios(id) ON DELETE CASCADE, reference_month YYYY-MM, payment_date YYYY-MM-DD, amount_paid_cents, payment_type, amortization_strategy, notes, created_at). Indexes `idx_scenarios_user` (user_id) and `idx_payments_scenario_month` (scenario_id, reference_month). Money in integer cents, rates in basis points.
  - `docs/schema.md`: appended a "0002_domain.sql" section documenting every column, type, FK, and index for both tables, with a note explaining the snake_case vs camelCase split.
- Verified: `bunx --bun wrangler d1 migrations apply calculadora-financeira-db --local` reports both migrations ✅; `SELECT sql FROM sqlite_master` confirms the DDL matches exactly; `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (356/356) ✓, `bun run build` ✓, `bunx --bun wrangler deploy --dry-run` ✓ (Total Upload still 1505 KiB — no code shipped this story).
- **Learnings for future iterations:**
  - The `db:migrate:local` npm script (`wrangler d1 migrations apply ...`) fails in this sandbox because it invokes the bare `wrangler` binary against Node 20. Use `bunx --bun wrangler d1 migrations apply <db> --local` instead — same effect, runs through Bun's bundled runtime. We may want to update the script to `bunx --bun wrangler ...` later (touchy because CI runs on Node 22 where `wrangler` works directly).
  - Cross-namespace foreign keys (snake_case child → camelCase parent) work fine in D1 because FK resolution uses the *quoted column name in the parent table* (`"id"`), not naming conventions. The migrations live happily side by side.
  - When the parent table is in a previously-applied migration, wrangler reports the new migration with a 🕒 spinner while running and ✅ once committed — both lines appear in a single `apply` run, so don't be alarmed by the interim status.
  - `migrations_dir` in `wrangler.jsonc` is relative to the project root, not to `src/`. Adding additional migrations is "drop a file matching `NNNN_<name>.sql` in that dir" — no manifest, no registration.
---

## 2026-05-18 - US-005
- Added Resend wiring for transactional emails and connected it to Better Auth's `sendVerificationEmail` and `sendResetPassword` hooks.
- Files changed:
  - `src/server/email.ts` (new): `sendEmail({to,subject,html,text}, env)` instantiates `new Resend(env.RESEND_API_KEY)` and calls `resend.emails.send({ from: env.EMAIL_FROM, ... })`. Empty/missing API key → `console.info` with payload preview and returns (dev mode). On `error` from Resend → `console.error` with structured metadata (`errorName`, `errorMessage`, `statusCode`) and throws `Error("Resend send failed: <name> — <message>")`.
  - `src/server/email-templates.ts` (new): `verifyEmailTemplate({url,name})` and `resetPasswordTemplate({url,name})`, both returning `{subject, html, text}` in pt-BR. HTML/url tokens passed through a small `escapeHtml` helper; reset template mentions "expira em 1 hora" to match `resetPasswordTokenExpiresIn: 3600` in `sharedAuthOptions`.
  - `src/server/auth.ts`: spread `sharedAuthOptions` and override `emailVerification` (whole block — shared options doesn't carry it) and `emailAndPassword` (spread + add `sendResetPassword`). Both hooks pass `user.name` through to the template so the greeting is "Olá, <name>" when present, "Olá" otherwise.
  - `src/server/env.d.ts`: added optional `RESEND_API_KEY?: string`. Optional intentionally — dev runs without it.
  - `src/server/email.test.ts` (new): 3 vitest cases covering (a) with key → constructor called with key + `emails.send` called with the merged payload, (b) without key → no constructor/send call + `console.info` logged once, (c) Resend returns `{error}` → `console.error` logged with structured payload + `sendEmail` rejects with informative message.
  - `package.json`: added `resend` dep (v6.12.3).
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (359/359, +3 new) ✓, `bun run build` ✓, `bunx --bun wrangler deploy --dry-run` ✓ (worker bundle grew to 1955 KiB — Resend SDK adds ~450 KiB, expected).
- **Learnings for future iterations:**
  - `sharedAuthOptions` is intentionally env-agnostic so the CLI (`auth.config.ts`) can use it. Anything that needs runtime bindings — Resend API key, EMAIL_FROM, Turnstile secret, future R2/KV stuff — gets layered on inside `createAuth(env)`. Don't try to push env-aware callbacks into the shared block.
  - When wrapping `emailAndPassword`: spread `sharedAuthOptions.emailAndPassword` first so `enabled`, `requireEmailVerification`, `resetPasswordTokenExpiresIn` survive. When wrapping `emailVerification`: define it whole — the shared block doesn't currently carry it, and spreading `undefined` is a TS error against `BetterAuthOptions`.
  - Better Auth's `sendVerificationEmail` and `sendResetPassword` callbacks receive `{ user, url, token }`. The `user` object has `name` (string), `email` (string), `id` (string). The url is the pre-built link that already includes the token, so the template doesn't need to construct it.
  - Templates returning `{subject, html, text}` (instead of just `{html, text}`) make the call site uniform — the caller just passes the three fields through to `sendEmail`. Avoids subject-strings being hardcoded in `auth.ts`.
  - Resend's `.emails.send()` does NOT throw on API errors — it returns `{ data: null, error: {name, message, statusCode} }`. Tests must mock with `mockResolvedValueOnce({data:null, error:{...}})`, not `mockRejectedValueOnce`. Network/timeouts would throw, but those bubble up via the normal promise chain.
  - The Resend SDK pulled in `render_resend` as a tiny module chunk (0.06 KiB) plus the main 450 KiB. Bundle is now 1955 KiB — still well below Workers' 10 MiB cap, but watch this as more SDKs land.
---

## 2026-05-19 - US-006
- Added Cloudflare Turnstile server-side verification and wired it into Better Auth's sign-up/sign-in flow as a `before` hook.
- Files changed:
  - `src/server/turnstile.ts` (new): `verifyTurnstile(token, env, remoteIp?)` POSTs `application/x-www-form-urlencoded` (URLSearchParams) to `https://challenges.cloudflare.com/turnstile/v0/siteverify` with `secret`, `response`, optional `remoteip`. Returns `true` when `env.TURNSTILE_SECRET_KEY` is empty (dev bypass + `console.warn`); returns `false` for empty token, success=false (with `console.warn` including the error-codes), or any thrown error (caught + `console.error`, fail-closed).
  - `src/server/env.d.ts`: added optional `TURNSTILE_SECRET_KEY?: string` (optional to mirror RESEND_API_KEY — dev runs without it).
  - `src/server/auth.ts`: imported `APIError, createAuthMiddleware` from `better-auth/api`; added `hooks.before` that matches `ctx.path ∈ {"/sign-up/email","/sign-in/email"}`, reads `ctx.body.turnstileToken`, derives `remoteIp` from `cf-connecting-ip` then first hop of `x-forwarded-for`, calls `verifyTurnstile`, and on failure throws `new APIError("BAD_REQUEST", { message: "Verificação anti-bot inválida. Recarregue a página e tente novamente.", code: "TURNSTILE_INVALID" })`. Hook is closure-bound to the per-request `env` so the secret key is per-Worker-request.
  - `src/server/turnstile.test.ts` (new): 6 vitest cases — success path with payload assertions (URL, method, content-type, body params); siteverify success=false → false; fetch throw → false; empty key → true + warn + no fetch call; empty token → false + no fetch call; remoteip propagated.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (365/365, +6 new) ✓, `bun run build` ✓, `bunx --bun wrangler deploy --dry-run` ✓ (worker bundle 1957 KiB — no growth, Turnstile uses native fetch).
- **Learnings for future iterations:**
  - `better-auth/api` is the public re-export for `createAuthMiddleware` and `APIError`. Internal modules import `APIError` from `@better-auth/core/error` but that path doesn't re-export it; the official import is the `better-auth/api` subpath (declared in package.json exports).
  - In a before-hook, `ctx.body` is the body parsed by the router (`getBody` in better-call's router.mjs) BEFORE the endpoint's zod schema runs. So unknown fields like `turnstileToken` are available even on endpoints whose validators would otherwise strip them (sign-in/email's schema is a closed `z.object({...})` — would strip — but the hook still sees it). Confirmed by reading `to-auth-endpoints.mjs:74` (runBeforeHooks runs against the rawContext.body before `createInternalContext` re-runs validation).
  - `vi.spyOn(globalThis, "fetch")` MUST be created inside `beforeEach` after `vi.restoreAllMocks()` (or as the first thing in each test). Capturing the spy at the top of a test, then calling `vi.spyOn` again later, gives you back the same handle — but `restoreAllMocks` in a prior `beforeEach`/`afterEach` orphans the captured reference. Pattern: module-scope `let fetchSpy: ReturnType<typeof vi.spyOn<typeof globalThis, "fetch">>;` initialized in `beforeEach`.
  - Cloudflare Turnstile siteverify accepts both JSON and form-encoded bodies. `URLSearchParams` is the simplest, avoids escaping issues, and matches the SDKs' default. Setting `content-type: application/x-www-form-urlencoded` explicitly is harmless and makes the call uniform across runtimes.
  - The remote-IP header to prefer in Workers is `cf-connecting-ip` (always present on Cloudflare-fronted requests). `x-forwarded-for` is the safe fallback for non-CF dev environments; only the first hop is meaningful (so split on "," and trim).
  - Better Auth's top-level `hooks.before` accepts a single middleware (not an array). For multiple before-conditions, branch by `ctx.path` inside one middleware (matches the pattern used by plugins like anonymous). Plugins use `hooks: { before: [{ matcher, handler }] }` instead — different shape.
---

## 2026-05-19 - US-007
- Added the Better Auth React client and a `useCurrentUser` hook for the frontend.
- Files changed:
  - `src/lib/auth-client.ts` (new): imports `createAuthClient` from `better-auth/react`; resolves baseURL from `import.meta.env.VITE_APP_URL`, falls back to `window.location.origin`, then to `http://localhost:5173` for SSR/test contexts. Exports `authClient`, plus convenience re-exports of `useSession`, `signIn`, `signUp`, `signOut`, and types `SessionData`, `AuthUser`, `AuthSession` derived from `ReturnType<typeof authClient.useSession>['data']`.
  - `src/lib/use-current-user.ts` (new, plain `.ts` per PRD): exports `useCurrentUser()` wrapping `authClient.useSession()`. Returns `{ user: AuthUser | null, isLoading: boolean, error: Error | null }`. Maps Better Auth's `isPending` → `isLoading` to match the PRD's API contract, and normalises `error: undefined | null` to always `null`.
  - `src/lib/use-current-user.test.ts` (new): 4 vitest cases — (1) session present → user object surfaced, isLoading=false, error=null; (2) data null → user null; (3) isPending → isLoading true; (4) error propagated through. Mocks `./auth-client` via a module-scope `useSessionMock = vi.fn()` returned from inside the `vi.mock` factory so the real `createAuthClient` is never instantiated.
- `.env.example` already had `VITE_APP_URL=http://localhost:5173` (added in US-002) — no change needed.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (369/369, +4 new) ✓, `bun run build` ✓ (dist/client/assets/index-*.js grew from 678 KiB → 817 KiB because the React client is now in the bundle — expected; Better Auth's React surface pulls in nanostores + @better-fetch/fetch).
- **Learnings for future iterations:**
  - The Better Auth React hook is `isPending`, not `isLoading`. Our wrapper renames it for consistency with the PRD's `{ user, isLoading, error }` shape (and to leave room for adding `isRefetching` later without overloading the term). Downstream consumers (ProtectedRoute in US-008, nav-user in US-015) should use the wrapper, not raw `authClient.useSession`.
  - Don't reach for `authClient.$Infer.Session` to type the user — the React client (`better-auth/react`) doesn't expose `$Infer`; only the vanilla client (`better-auth/client`) does. Use `ReturnType<typeof authClient.useSession>['data']` instead, which works on both flavours.
  - `baseURL` fallback chain matters for SSR/test: `VITE_APP_URL` first (defined in .env / Vite define), `window.location.origin` next (browser), `http://localhost:5173` last (jsdom doesn't always set a location.origin on first import). The function form (`resolveBaseURL()`) evaluates these lazily so the module-load order is safe.
  - For jsdom tests, mock the entire `./auth-client` module rather than spying on `createAuthClient`. Better Auth's React client constructor wires up nanostores / BroadcastChannel / online-manager / focus-manager listeners at instantiation time; mocking the module saves jsdom from booting those and keeps the test suite fast.
---

## 2026-05-19 - US-008
- Added react-router-dom client routing with public auth routes, an `AppShell` layout, and a `ProtectedRoute` gate.
- Files changed:
  - `src/app/App.tsx`: now just `<BrowserRouter><AppRoutes/></BrowserRouter>`. All route definitions moved out of this file.
  - `src/app/routes.tsx` (new): `<AppRoutes/>` exporting the full route tree. `/` redirects to `/financiamento`; `/login|/register|/verify-email|/forgot-password|/reset-password` render under `<AuthLayout/>`; `/financiamento` + `/alugar-x-financiar` render under `<AppShell/>`; `/historico` additionally wrapped by `<ProtectedRoute/>`; catch-all `*` → `<NotFoundPage/>`.
  - `src/app/app-shell.tsx` (new): placeholder shell preserving the MVP's sticky header + ThemeToggle + max-w-screen-xl main. Renders `<Outlet/>`. Will be replaced by the shadcn dashboard-01 layout in US-013.
  - `src/app/auth-layout.tsx` (new): clean centered layout (no sidebar) with "Calculadora Financeira" branding header. Renders `<Outlet/>` for auth pages.
  - `src/app/protected-route.tsx` (new): reads `useCurrentUser()`. `isLoading` → status spinner "Carregando…"; `!user` → `<Navigate to="/login" replace state={{ from: location.pathname }}/>` so the post-login redirect target is preserved; otherwise `<Outlet/>`.
  - `src/app/not-found-page.tsx` (new): 404 with a "Voltar para o simulador" link to `/financiamento`.
  - `src/features/simulator/pages/financiamento-page.tsx` (new): contains the current simulator content (Premissas/Financiamento/Pagamento extra/Aluguel vs compra/Resumo/Tabela/Gráficos/Exportar). Moved verbatim from `App.tsx` minus the now-shared header.
  - `src/features/simulator/pages/alugar-x-financiar-page.tsx` (new): placeholder card pt-BR.
  - `src/features/historico/pages/historico-page.tsx` (new): placeholder card pt-BR.
  - `src/features/auth/pages/login-page.tsx`, `register-page.tsx`, `verify-email-page.tsx`, `forgot-password-page.tsx`, `reset-password-page.tsx` (new): placeholder cards pt-BR. Real forms will land in USs 9–12.
  - `src/app/App.test.tsx`: rewritten as a router test. Mocks `@/lib/auth-client` with a module-scope `useSessionMock` so the real Better Auth React client never instantiates. 8 cases: root redirects to /financiamento, /financiamento public, /alugar-x-financiar public, /historico → /login when anon, /historico shows loading when isPending, /historico renders content when authed, /login renders under auth layout (no sticky shell header), unknown route → 404.
  - `src/features/simulator/pages/financiamento-page.test.tsx` (new): inherits the original App.test.tsx assertions (header/title, 8 section cards, empty-state text, theme tokens, sticky header, 12-col grid). Rendered with `<MemoryRouter><AppShell/><FinanciamentoPage/></MemoryRouter>` so the shell chrome is exercised alongside the page.
  - `package.json`: + `react-router-dom@7.15.1`.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (377/377, +8 routing + +6 financiamento-page vs the old 6 App.test) ✓, `bun run build` ✓ (client bundle 817 KiB → 886 KiB — react-router-dom adds ~70 KiB, expected).
- Browser verification was NOT performed in this sandbox (no MCP browser tool present in this iteration). The PRD acceptance criterion "Verify in browser using dev-browser skill" should be re-checked manually when a browser-enabled iteration runs.
- **Learnings for future iterations:**
  - `react-router-dom@7` still supports the classic `<BrowserRouter><Routes>` API alongside the new data-router API (`createBrowserRouter`). The PRD specifies `<BrowserRouter>`; choosing it keeps the migration small and tests trivially wrappable in `<MemoryRouter>`. If we ever need loaders/actions (US-030+ may want them), we can migrate to `createBrowserRouter` without rewiring every page.
  - The `AppRoutes` factoring (separate from `App.tsx`'s `<BrowserRouter>`) is the canonical pattern for testable routers. Wrapping `<App/>` in a `<MemoryRouter>` would nest two routers and silently break navigation; always test against the route *table*, not the router.
  - `<ProtectedRoute>` as a layout route (with `<Outlet/>`) composes cleanly with other layout routes — `<Route element={<AppShell/>}><Route element={<ProtectedRoute/>}><Route path="/historico" .../></Route></Route>` runs the shell + gate + page in order. Avoid the alternative pattern of wrapping each page individually; the layout-route form is what react-router optimises for (single render per nested level).
  - When pages need access to "where the user was trying to go" (for post-login redirect in US-010), pass it through `<Navigate state={{from}}>` and read `useLocation().state` in the login page. We pre-populate this in ProtectedRoute now so US-010 doesn't have to refactor it.
  - Tests that involve the auth client MUST mock `@/lib/auth-client` (not just `./auth-client` — vitest's `vi.mock` matches the exact import path, and our pages import via the `@/` alias). Pattern: `vi.mock("@/lib/auth-client", () => ({ authClient: { useSession: () => useSessionMock() } }))` with a module-scope `useSessionMock = vi.fn()`.
---

## 2026-05-19 - US-009
- Implemented the /register page with the shadcn Form pattern, Cloudflare Turnstile widget, and Better Auth sign-up wiring. Added the /check-email destination page for the post-submit flow.
- Files changed:
  - `src/components/ui/form.tsx` (new): hand-written shadcn `Form` primitives — `Form` (alias for `FormProvider`), `FormField` (wraps `Controller` and provides a name context), `FormItem` (creates an id context with `useId`), `FormLabel` (uses our existing `Label` + auto-links via `htmlFor`), `FormControl` (uses `@radix-ui/react-slot` + `aria-describedby` + `aria-invalid`), `FormDescription`, `FormMessage` (auto-renders the field's zod error), and `useFormField` hook. Standalone — no `@radix-ui/react-label` dep.
  - `src/features/auth/schemas/register.ts` (new): zod schema with pt-BR messages: email (required, valid), password (≥ 8 chars), passwordConfirmation (matches password, enforced via `superRefine` so the error attaches to `passwordConfirmation`). Exports `registerSchema` + `RegisterFormValues`.
  - `src/features/auth/components/turnstile-field.tsx` (new): wraps `<Turnstile/>` from `@marsidev/react-turnstile`. Reads `import.meta.env.VITE_TURNSTILE_SITE_KEY`; if empty, renders a small notice and fires `onToken('dev-token-no-site-key')` once on mount. If present, mounts the real widget with `theme: 'auto'`, `language: 'pt-BR'`. Exposes `onToken/onExpire/onError`.
  - `src/features/auth/pages/register-page.tsx` (rewritten): form with email/senha/confirme a senha + Turnstile widget + submit. Submit stays disabled until both `!submitting` and `turnstileToken` truthy. On submit calls `authClient.signUp.email({email,password,name:email,callbackURL:'/login', fetchOptions:{body:{turnstileToken}}})` — the extra field goes through `fetchOptions.body` because the strict TS type of `signUp.email` doesn't allow unknowns on the main arg. Success → `navigate('/check-email', {replace:true})`. Errors mapped pt-BR via `translateSignUpError` (`USER_ALREADY_EXISTS`, `PASSWORD_TOO_SHORT`, `TURNSTILE_INVALID`, 429, else `error.message`). Footer link "Já tem conta? Entrar" → `/login`.
  - `src/features/auth/pages/check-email-page.tsx` (new): simple "Verifique seu email" card with a 'reenviar' email input + button calling `authClient.sendVerificationEmail({email, callbackURL:'/login'})`. Status state machine (`idle | sending | sent | error`) with pt-BR strings.
  - `src/app/routes.tsx`: added `/check-email` route under the existing `<AuthLayout/>` group, between `/register` and `/verify-email`.
  - `src/features/auth/pages/register-page.test.tsx` (new): 7 vitest cases — renders fields + turnstile + login link; submit disabled until turnstile; password mismatch validation; password too short; happy path submits with correct payload AND navigates to /check-email; `USER_ALREADY_EXISTS` → pt-BR error; `TURNSTILE_INVALID` → pt-BR error. Mocks `@/lib/auth-client` and `@/features/auth/components/turnstile-field` so jsdom never boots the real Better Auth client or Turnstile script.
  - `src/features/auth/CLAUDE.md` (new): pattern notes for sibling auth pages (forms, turnstile, error translation, testing).
  - `package.json` + `bun.lock`: + `@marsidev/react-turnstile@1.5.2`.
  - `.env.example` already had `VITE_TURNSTILE_SITE_KEY` (added in US-006) — no change.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (384/384, +7 register-page tests) ✓, `bun run build` ✓ (client bundle 886 KiB → 894 KiB — adds the Turnstile React wrapper, form primitives, and the new pages).
- Browser verification was NOT performed in this sandbox (no MCP browser tool present). The PRD acceptance "Verify in browser using dev-browser skill" should be re-checked manually when a browser-enabled iteration runs.
- **Learnings for future iterations:**
  - The strict-typed `authClient.signUp.email` / `signIn.email` reject unknown body fields at compile time even though the runtime endpoint accepts them (sign-up's body schema is `z.object(...).and(z.record(z.string(), z.any()))` server-side). Pass extras via `fetchOptions.body` — the client proxy merges them. This pattern will recur for US-010 (sign-in + turnstile token) and any future protected-action endpoints.
  - When `bunx shadcn@latest add <component>` fails offline (sandbox has no network), the component can be hand-written following the upstream shape. Validate against `node_modules/@radix-ui/*` to know which deps the standard component needs; we substituted our local `Label` for `@radix-ui/react-label` to avoid pulling a new dep. The hand-written `form.tsx` is API-compatible with shadcn's so future copy/paste examples from docs work unchanged.
  - `@marsidev/react-turnstile` exports `Turnstile` (forwardRef component) + `TurnstileInstance` (imperative methods) + types. The `language: 'pt-BR'` option works (pt-BR is in `TurnstileLangCode`). The widget calls `onSuccess(token)` once the challenge resolves; tokens are short-lived (~5 min by Cloudflare default), so listen to `onExpire` and clear local state to re-disable submit.
  - The dev bypass for Turnstile lives on BOTH sides: the frontend `TurnstileField` fires a placeholder token when the site key env is empty, and `verifyTurnstile` on the Worker (US-006) returns `true` when the secret key env is empty. Both checks key off "env is empty," not on a separate `NODE_ENV === 'development'` flag — keeps dev/prod parity simple.
  - For Better Auth client error handling: `result.error` is typed as `{ code?: string; message?: string }` (see `InferRoute` in `path-to-object.d.mts:59`). Always check `code` first (stable enum-like string), fall back to `message` (English by default — translate to pt-BR before showing). `status` (HTTP code) is also present at runtime via `BetterFetchError.status` but isn't in the strict type, so cast or use `(error as {status?: number}).status` to map 429 → rate-limit message.
  - When mocking the `authClient` in tests, mock the whole module with only the methods used (e.g. `authClient.signUp.email`) — don't try to mock the full client. Reach into nested properties via the factory: `{ authClient: { signUp: { email: (...args) => mock(...args) } } }`. Spreading a real-looking `authClient` is unnecessary and pulls in nanostores.
---

## 2026-05-19 - US-010
- Implemented the /login page with the shadcn Form pattern, Cloudflare Turnstile widget, and Better Auth sign-in wiring. Post-login redirect honours the ProtectedRoute `location.state.from` so deep links bounced through the gate resume to their original destination.
- Files changed:
  - `src/features/auth/schemas/login.ts` (new): zod schema with pt-BR messages — email required+valid, password required (no min length on sign-in; server enforces).
  - `src/features/auth/pages/login-page.tsx` (rewritten from placeholder): form with email/senha + "Esqueci minha senha" link + Turnstile widget + submit. Submit disabled until both `!submitting` and `turnstileToken` truthy. Calls `authClient.signIn.email({ email, password, callbackURL: redirectTarget, fetchOptions: { body: { turnstileToken } } })` where `redirectTarget` is `location.state.from` (if set and starts with "/") or `/financiamento`. Success → `navigate(redirectTarget, { replace: true })`. Errors classified into 5 kinds via `classifySignInError`: `invalid_credentials` (INVALID_EMAIL_OR_PASSWORD / INVALID_PASSWORD / USER_NOT_FOUND), `email_not_verified` (EMAIL_NOT_VERIFIED / USER_EMAIL_NOT_VERIFIED — renders a "Reenviar email de verificação" button that calls `authClient.sendVerificationEmail({email, callbackURL:'/login'})` with idle/sending/sent/error status), `rate_limit` (status 429), `turnstile` (TURNSTILE_INVALID), `generic` (anything else). Footer link "Não tem conta? Criar conta" → `/register`.
  - `src/features/auth/pages/login-page.test.tsx` (new): 9 vitest cases — renders fields + turnstile + nav links; submit disabled until turnstile; email validation; happy path submits with correct payload AND navigates to /financiamento; preserves `location.state.from` (e.g. `/historico`) as `callbackURL` and as the post-login destination; INVALID_EMAIL_OR_PASSWORD → pt-BR error; EMAIL_NOT_VERIFIED → message + working "reenviar" button (calls sendVerificationEmail w/ correct args, shows "Email reenviado"); status 429 → rate-limit pt-BR; TURNSTILE_INVALID → pt-BR error. Mocks `@/lib/auth-client` for signIn.email + sendVerificationEmail and `@/features/auth/components/turnstile-field` with a stub button that fires `onToken`.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (393/393, +9 login-page tests) ✓, `bun run build` ✓ (client bundle 894 → 898 KiB — adds the login page and login schema; no new deps).
- Browser verification was NOT performed in this sandbox (no MCP browser tool present). The PRD acceptance "Verify in browser using dev-browser skill" should be re-checked manually when a browser-enabled iteration runs.
- **Learnings for future iterations:**
  - The post-login redirect contract is split across **three** files and they need to agree: (a) `ProtectedRoute` writes `state.from = location.pathname` when bouncing to `/login`; (b) `LoginPage` reads `useLocation().state.from`, validates it (`startsWith("/") && !startsWith("//")` to avoid open-redirect via protocol-relative URLs), and passes it as both the `callbackURL` AND the post-success `navigate(target, {replace:true})` target; (c) any future link to `/login` from elsewhere can opt-in to this by `navigate('/login', {state:{from:...}})` or `<Link to="/login" state={{from:...}}>`. Default fallback `/financiamento` keeps direct visits to `/login` working.
  - Better Auth's `sendVerificationEmail` is on the React client at the top level (`authClient.sendVerificationEmail`), NOT under a sub-object like `signIn.email`. Signature: `({ email, callbackURL })` → `{ data, error }`. Use it for the "reenviar" pattern on /login (email-not-verified) and on /check-email (US-009 already uses this).
  - Better Auth's email-not-verified error code is `EMAIL_NOT_VERIFIED` in the public error map (see `better-auth/dist/api/index.d.mts`). Some Better Auth versions also surface `USER_EMAIL_NOT_VERIFIED`. Handle both defensively — the cost is one extra `||` in the classifier and avoids a silent regression on a minor bump.
  - The classifier returns a *kind* (sum type) rather than a translated string, because the email-not-verified branch needs additional UI (the resend button) — not just a different message. Single-string `translateXxxError` (US-009 register pattern) is fine when all branches are "just text"; reach for the kind-based variant when one branch needs a richer render.
  - For `useLocation().state` reads in TypeScript, always go through a type guard (`isLocationStateWithFrom`). React-router types `state` as `unknown` (correctly — anything can be passed via history.state), and `state?.from as string` would happily eat a non-string value at runtime. The guard + the `/`-prefix validation also protects against open-redirect via `state.from = "//attacker.example.com/path"`.
---

## 2026-05-19 - US-011
- Implemented the /verify-email page that reads the token from the query string, calls `authClient.verifyEmail({ query: { token } })` on mount, and on success shows a pt-BR confirmation + auto-navigates to `/login` after 2s. On error/missing-token it renders a pt-BR explanation and the "Reenviar email de verificação" form (email input + button calling `authClient.sendVerificationEmail({email, callbackURL:'/login'})`).
- Files changed:
  - `src/features/auth/pages/verify-email-page.tsx` (rewritten from US-008 placeholder): uses `useSearchParams` to read `token`; tracks state with a `VerifyStatus` union (`verifying | success | missing-token | error`) and a separate `ResendStatus` union (`idle | sending | sent | error`). Mount-effect guards re-runs with a `verifyStartedRef` (React 19 StrictMode double-mount) and cleans up the redirect timeout on unmount. `translateVerifyError` maps `INVALID_TOKEN`/`TOKEN_EXPIRED`/`INVALID_VERIFICATION_TOKEN` to a pt-BR string; falls back to `error.message` if non-empty/non-"null"; otherwise a generic pt-BR message. Resend form is rendered for both `error` and `missing-token`; submit without an email yields an inline pt-BR validation message.
  - `src/features/auth/pages/verify-email-page.test.tsx` (new): 7 vitest cases — verifyEmail called once with the token from the query; success shows the confirmation and redirects to `/login` after advancing fake timers 2000 ms; invalid/expired token → pt-BR message + resend form rendered; missing-token state surfaces the pt-BR notice + resend form, and does not call verifyEmail; resend form submits with `{email, callbackURL:'/login'}` and shows "Email reenviado"; resend without email yields the inline validation; thrown error from verifyEmail surfaces the error message and renders the resend form. Mocks `@/lib/auth-client` with module-scope `vi.fn()` stubs for `verifyEmail` + `sendVerificationEmail`.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (400/400, +7 verify-email tests, going from 393 → 400) ✓, `bun run build` ✓ (client bundle 898 → 902 KiB — adds the verify-email page logic + Button/Input/Label usage; no new deps).
- Browser verification was NOT performed in this sandbox (no MCP browser tool present). The PRD acceptance "Verify in browser using dev-browser skill" should be re-checked manually when a browser-enabled iteration runs.
- **Learnings for future iterations:**
  - `authClient.verifyEmail` lives at the top level of the React client (NOT `authClient.signIn.email`-style nesting), and the canonical signature is `{ query: { token } }`. The `query` wrapper matters — `authClient.verifyEmail({ token })` compiles but silently sends the token as a body field, which the server's GET /verify-email endpoint ignores.
  - To unit-test "navigates to /login after 2 seconds" with vitest, use `vi.useFakeTimers({ shouldAdvanceTime: true })` in `beforeEach` (so async work inside the effect resolves naturally) and `await act(async () => { await vi.advanceTimersByTimeAsync(2000); })` in the test. Wrapping in `act` is required because the timer firing causes a `navigate()` → React state update; without `act` the test would either flake or warn about "not wrapped in act".
  - React 19's StrictMode double-mounts effects in dev. Guard idempotent side-effects (one-time token-verification on mount) with a `useRef(false)` set on first execution — fires once even under double-mount. The cleanup function then cancels the in-flight promise via a closed-over `cancelled` flag and clears the redirect timeout.
  - Better Auth's `verifyEmail` errors arrive with `code` set to `INVALID_TOKEN` or `TOKEN_EXPIRED` (some minor versions emit `INVALID_VERIFICATION_TOKEN`). Translate all three to the same "link inválido ou expirado" message — there's no actionable UI difference between "wrong token" and "old token" from the user's POV; both branches show the resend form.
  - When implementing pages that auto-navigate post-success, the previous-page check via `expect(screen.queryByText(/login page/i)).not.toBeInTheDocument()` BEFORE advancing fake timers is the right way to assert the redirect is delayed (not immediate). Just-advancing-and-asserting can pass even if the navigation fires synchronously, hiding a regression.
---


## 2026-05-19 - US-012
- Implemented the `/forgot-password` and `/reset-password` pages, end-to-end, with the shadcn `<Form>` + zod pattern.
- Files changed:
  - `src/features/auth/schemas/forgot-password.ts` (new): single-field zod schema (email required + valid, pt-BR messages).
  - `src/features/auth/schemas/reset-password.ts` (new): `password` (≥8) + `passwordConfirmation` with `superRefine` mismatch check (pt-BR).
  - `src/features/auth/pages/forgot-password-page.tsx` (rewritten from placeholder): form posts to `authClient.requestPasswordReset({email, redirectTo:'/reset-password'})`. Success swaps the card content for the generic pt-BR message ("Se o email existir, enviaremos um link para redefinir a senha.") + a hint that the link expires in 1h. Errors map status 429 → "Muitas tentativas…"; other failures → "Não foi possível enviar o email agora…"; network rejection → "Erro de rede…". Footer link to `/login`.
  - `src/features/auth/pages/reset-password-page.tsx` (rewritten from placeholder): reads `?token` and `?error` from query string. If `?error=INVALID_TOKEN` or missing token → renders an explanatory pt-BR alert + a "Solicitar novo link" link to `/forgot-password` + "Voltar para entrar" link, with the form hidden. Otherwise renders the new-senha + confirme form; submit calls `authClient.resetPassword({newPassword, token})`. Success → `navigate('/login?flash=Senha%20redefinida%20com%20sucesso.%20Entre%20com%20sua%20nova%20senha.')`. Server-side `INVALID_TOKEN`/`TOKEN_EXPIRED` flips into the "request new link" UI; `PASSWORD_TOO_SHORT` and rate-limit get pt-BR strings; unknown codes fall back to `error.message`.
  - `src/features/auth/pages/login-page.tsx`: added a `useSearchParams` read for `?flash`. When set, renders an emerald-tinted, `aria-live="polite"` status banner above the form. Consumed by /reset-password's success redirect — no other caller produces flash messages yet.
  - `src/features/auth/pages/forgot-password-page.test.tsx` (new): 8 vitest cases — render fields/links; required-email validation; invalid-email validation; happy path submits the right payload + shows the generic success message; the generic message is identical regardless of email (no leak); rate-limit (status 429) → pt-BR; unknown server failure → pt-BR generic; network rejection → "Erro de rede…".
  - `src/features/auth/pages/reset-password-page.test.tsx` (new): 10 vitest cases — render with token; missing token → "request new link" UI; `?error=INVALID_TOKEN` → "request new link" UI; mismatched passwords inline validation; short password inline validation; happy path submits `{newPassword, token}` and navigates to /login (with flash); server `INVALID_TOKEN` → switches to "request new link"; `PASSWORD_TOO_SHORT` → pt-BR; status 429 → pt-BR; unknown code → echoes server message.
  - `src/features/auth/pages/login-page.test.tsx`: +1 case asserting that `?flash=...` is rendered above the form.
- Reset-token TTL: already at 1h via `resetPasswordTokenExpiresIn: 60 * 60` in `src/server/auth-options.ts` (set in US-002). No server-side change needed for US-012's TTL criterion.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (419/419, +18 new vs the prior 400 + +1 login flash) ✓, `bun run build` ✓ (client bundle 902 → 907 KiB — adds forgot/reset pages, schemas, and the flash banner; no new deps).
- Browser verification was NOT performed in this sandbox (no MCP browser tool present). Acceptance "Verify in browser using dev-browser skill" should be re-checked when a browser-enabled iteration runs.
- **Learnings for future iterations:**
  - The PRD's wording `authClient.forgetPassword(...)` doesn't match Better Auth v1.6.x. The actual endpoint is `POST /request-password-reset`, exposed on the client as `authClient.requestPasswordReset({email, redirectTo})`. The `forgetPassword` alias only exists on the OTP plugin (`authClient.forgetPassword.emailOtp(...)`), which we're not using. Use `requestPasswordReset` for the email/password reset flow.
  - Better Auth's `requestPasswordReset` always returns `{ status: true, message: "..." }` for unknown emails (timing-attack mitigated server-side). So `result.error` will only fire on rate-limit (429) or genuine server faults — never on "email doesn't exist." This makes the "don't leak account existence" criterion trivial to satisfy: show the generic success copy on `!result.error` and any other error is genuinely non-account-related.
  - `redirectTo` in `requestPasswordReset` accepts relative paths (e.g. `/reset-password`) because the server's origin-check middleware uses `allowRelativePaths: true` for redirect labels. We don't need to pass a full URL — the server's `requestPasswordResetCallback` redirects to `${redirectTo}?token=${token}` against the request's baseURL.
  - When testing pages with two fields whose labels share a suffix (`Nova senha` + `Confirme a nova senha`), `getByLabelText(/nova senha$/i)` matches BOTH and throws "Found multiple elements." Use `/^nova senha$/i` to anchor on both ends. Same pattern from the register-page's `senha` + `confirme a senha` — the existing tests use `/^senha$/i` and `/confirme a senha/i` (the latter has no anchor collision because "confirme" is unique).
  - TypeScript's flow analysis DOES narrow aliased boolean expressions across destructured variables. `const x = errorKind?.kind === "invalid_token"; const showX = a || x;` followed by `if (!showX)` correctly narrows `errorKind?.kind` to exclude `"invalid_token"` inside the branch. Don't write a redundant `errorKind.kind !== "invalid_token"` check inside the narrowed branch — it'll produce a TS2367 "no overlap" error.
  - The flash-querystring pattern (`/login?flash=<encodeURIComponent(text)>`) is simple, server-agnostic, and survives the SPA's `<Navigate replace/>`. Just `decodeURIComponent` is unnecessary because `URLSearchParams.get` already decodes. Render with `aria-live="polite"` so screen readers announce it. If multiple pages need flash messages in the future, factor `useFlashMessage()` into `src/lib/` — for now one consumer (login) doesn't justify the abstraction.
---

## 2026-05-19 - US-013
- Scaffolded the authenticated app shell from the shadcn `dashboard-01` block: sidebar (drawer on mobile), site header, nav main/documents/user, plus stubs for the block's content surfaces (section-cards, chart-area-interactive, data-table). Replaced the placeholder `AppShell` so `/financiamento`, `/alugar-x-financiar`, `/historico` now render under the dashboard layout with all their existing content intact.
- `bunx shadcn@latest add dashboard-01` failed offline (`ui.shadcn.com` unreachable); components were hand-written from the upstream sources and adapted to react-router-dom + our existing primitives.
- Files changed:
  - `src/components/ui/sidebar.tsx` (new): full upstream sidebar primitive — `SidebarProvider`, `Sidebar`, `SidebarTrigger`, `SidebarInset`, header/footer/content/group/menu/menu-button/menu-sub-button, mobile drawer via `Sheet`, cookie persistence, `Ctrl/Cmd+B` shortcut.
  - `src/components/ui/sheet.tsx`, `tooltip.tsx`, `dropdown-menu.tsx`, `avatar.tsx`, `separator.tsx`, `skeleton.tsx` (new): supporting Radix-based primitives the sidebar + dashboard-01 block depend on. All hand-written following shadcn new-york style.
  - `src/lib/use-mobile.ts` (new): `useIsMobile()` hook (max-width 768 px) used by `SidebarProvider` to switch between rail and drawer.
  - `src/components/app-sidebar.tsx` (new): branded sidebar wrapping `NavMain`, `NavDocuments`, `NavUser`. Items: Início (`/`), Financiamento, Alugar x Financiar, Histórico, plus Gráficos under Documentos.
  - `src/components/site-header.tsx` (new): top bar with `SidebarTrigger`, vertical `Separator`, h1 title "Simulador de Financiamento", `ThemeToggle` aligned right.
  - `src/components/nav-main.tsx`, `nav-documents.tsx` (new): each item is a `<NavLink>` driving `isActive` into `<SidebarMenuButton asChild isActive={isActive}>` so active-route highlighting matches the URL.
  - `src/components/nav-user.tsx` (new): reads `useCurrentUser()`. Anonymous → "Entrar" button → `/login`; authenticated → avatar + dropdown with "Conta" (disabled placeholder) + "Sair" (calls `authClient.signOut()` then navigates to `/login`). Initials helper falls back to email when name is empty.
  - `src/components/section-cards.tsx`, `chart-area-interactive.tsx`, `data-table.tsx` (new): minimal placeholder shells from the dashboard-01 block. The real simulator surfaces live under `src/features/simulator/components/*`; these stubs exist so future stories can swap them in without rippling imports.
  - `src/app/app-shell.tsx`: rewrote the placeholder shell as `SidebarProvider` + `AppSidebar variant="inset"` + `SidebarInset` containing `<SiteHeader/>` and the routed `<Outlet/>`. Sidebar persistent on desktop, drawer on mobile, auth routes bypass entirely (they use `<AuthLayout/>` in `src/app/routes.tsx`).
  - `src/components/CLAUDE.md` (new): module conventions — dashboard-01 shell adapts shadcn to react-router-dom; placeholders vs real domain charts/tables; `useCurrentUser` flow in `NavUser`.
  - `src/features/simulator/pages/financiamento-page.test.tsx`: updated the "sticky header" test to assert the new dashboard-01 header (`border-b` + `h-(--header-height)`), since the new `SiteHeader` is part of the `SidebarInset` flex column rather than a sticky viewport bar.
  - `package.json` + `bun.lock`: + `@tabler/icons-react@3.44.0`, `@radix-ui/react-dropdown-menu@2.1.16`, `@radix-ui/react-tooltip@1.2.8`, `@radix-ui/react-dialog@1.1.15` (for `Sheet`), `@radix-ui/react-separator@1.1.8`, `@radix-ui/react-avatar@1.1.11`.
- CSS tokens were already in place from earlier work (`src/index.css` defines `--sidebar`, `--sidebar-foreground`, `--sidebar-primary`, `--sidebar-accent`, `--sidebar-border`, `--sidebar-ring` for both light + dark, mapped to Tailwind utilities via `@theme inline`). No CSS changes were needed.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (419/419 — added zero net tests; the financiamento-page sticky-header test was rewritten in place to match the new shell) ✓, `bun run build` ✓ (client bundle 907 → 1060 KiB. The +150 KiB is mostly the new Radix primitives, Tabler icons, and Sheet/Tooltip/DropdownMenu state machines — expected for a shell-grade UI library; the rest of `dashboard-01`'s heavy deps (`@dnd-kit/*`, `vaul`, `sonner`, `@tanstack/react-table`) were intentionally NOT installed because their components are stubbed).
- Browser verification was NOT performed in this sandbox (no MCP browser tool present in this iteration). The PRD acceptance "Verify in browser using dev-browser skill" should be re-checked manually when a browser-enabled iteration runs — focus areas: sidebar collapses to drawer at ≤768 px, `Ctrl/Cmd+B` toggles desktop sidebar, dark-mode tokens stay coherent, /login + /register render without the shell.
- **Learnings for future iterations:**
  - The sidebar primitive's "active route" pattern depends on `<NavLink>` + `asChild`: `<NavLink to={url} end>{({isActive}) => <SidebarMenuButton asChild isActive={isActive}><span>…</span></SidebarMenuButton>}</NavLink>`. The trailing `end` prop matters — without it, `/financiamento` would stay active when on `/financiamento/anything-else`, and `/` would always be active. Use `end` for exact-match parents.
  - When stubbing the block's content components (`section-cards`, `chart-area-interactive`, `data-table`), keep the API surface minimal but stable: a `items`/`children`/`rows+columns` prop and a no-data branch. Future stories can replace internals (e.g. swap in `@tanstack/react-table` for `DataTable`) without touching call sites.
  - The new shell breaks any test that asserted the old MVP's sticky header. The pattern that survives the swap is asserting *what* the layout provides ("a `header` element with `border-b` exists") rather than *how* it positions itself ("sticky top-0 shadow"). Apply this when writing tests that touch the shell — assert role/structure, not Tailwind utility classes that are subject to layout-block churn.
  - `dashboard-01` and friends import from `next/link` / `next/navigation`. When porting upstream blocks to this Vite + react-router-dom codebase, the systematic substitutions are: `next/link` → `react-router-dom` (`Link`, `NavLink`); `usePathname()` → `useLocation().pathname` (or `<NavLink isActive>` callback); `useRouter().push(...)` → `useNavigate()(...)`. Keep an eye out for `Image` from `next/image` (none in this block, but other blocks use it — fall back to a plain `<img>`).
  - The CSS `--sidebar-*` tokens we shipped in earlier stories (US-001 / theme setup) were prescient — they let the new shell drop in without a single CSS edit. If a future shadcn block expects tokens we don't have (`--chart-area-grid`, `--ring-offset`, etc.), add them to `:root`, `.dark`, AND `@theme inline` so Tailwind utilities resolve. The `@theme inline` block is the one most often forgotten.
  - `useCurrentUser()` returns `{ user: null, isLoading: false }` for anonymous visitors on public routes (`/financiamento`, `/alugar-x-financiar`). `NavUser` must render gracefully in that state — the pattern here is to surface an "Entrar" button that points at `/login`. Don't lean on `ProtectedRoute` to keep `NavUser` from rendering anonymous — the shell wraps public routes too.
---

## 2026-05-19 - US-014
- Restructured the sidebar navigation to the three domain areas (Financiamento, Histórico, Alugar x Financiar), conditionally hiding Histórico for anonymous users, and removed the unused dashboard-01 extras.
- Files changed:
  - `src/components/app-sidebar.tsx`: replaced `navMain` with three domain items (Financiamento → IconCalculator, Histórico → IconHistory, Alugar x Financiar → IconScale). Removed the `Início` entry and the `IconHome` / `IconChartBar` imports. Pulls `useCurrentUser()` and filters `Histórico` out when `user === null`. Removed the `NavDocuments` import + group from `SidebarContent`. The header link still points to `/financiamento` with the "Calculadora Financeira" branding (unchanged). Sidebar still uses `collapsible="offcanvas"` so desktop collapses to icon rail and mobile opens the Sheet drawer (provided by the primitive).
  - `src/components/nav-documents.tsx`: deleted (no remaining importers).
  - `src/components/app-sidebar.test.tsx` (new): 7 vitest cases covering brand-link href, Financiamento + Alugar x Financiar always rendered, Histórico hidden when anon, Histórico visible when authed, legacy labels ("Documentos"/"Gráficos"/"Início") absent, NavLink active class applied for the current route, react-router hrefs (no full reload). Mocks `@/lib/auth-client` with a module-scope `useSessionMock`; stubs `window.matchMedia` for `useIsMobile` inside `SidebarProvider`.
  - `src/components/CLAUDE.md`: updated the layout-shell list (dropped `nav-documents.tsx`) and added a bullet documenting the `useCurrentUser`-driven filter for `Histórico` and the intentional removal of `navDocuments` / `navSecondary`.
- Quality checks: `bun run typecheck` ✓, `bun run lint` ✓, `bun run test` (426/426, +7 new sidebar tests vs the prior 419) ✓, `bun run build` ✓ (client bundle 1060 → 1058 KiB — small reduction from dropping `nav-documents.tsx` and `IconHome` / `IconChartBar`; no new deps).
- Browser verification was NOT performed in this sandbox (no MCP browser tool present). The PRD acceptance "Verify in browser using dev-browser skill" should be re-checked manually — focus areas: anonymous user sees Financiamento + Alugar x Financiar only; signing in reveals Histórico without a page reload; clicking each item updates the URL without a full reload; sidebar still collapses on Ctrl/Cmd+B and opens as a Sheet drawer on mobile.
- **Learnings for future iterations:**
  - The sidebar primitive does NOT re-render its content when the route changes — only the active-class on `<NavLink>` flips. So conditionally rendered items (here: `Histórico` gated on `user`) need to come from a hook that triggers a render when the session changes. `useCurrentUser()` (which wraps Better Auth's `useSession`) is the right hook because Better Auth's nanostore re-renders subscribers on session change. Don't try to filter inside `NavMain` itself — keep the gating where the `useCurrentUser` call lives (`AppSidebar`) so the items array prop stays a pure data slice.
  - When deleting a previously-exported component (`nav-documents.tsx`), `bun run typecheck` is the cheapest detector for leftover imports. Doing a single grep first (`grep -rn nav-documents src`) is faster but doesn't catch typed re-exports through `index.ts` barrels (we don't have any in `src/components/`, but the habit matters elsewhere).
  - `SidebarProvider` reads `window.matchMedia` via `useIsMobile()` on mount, so any test that renders the sidebar (not just `AppShell`) needs the matchMedia stub. The pattern from `App.test.tsx` / `theme-provider.test.tsx` is the canonical one: `Object.defineProperty(window, "matchMedia", { configurable, writable, value: () => ({ matches, media, onchange, addEventListener, removeEventListener, addListener, removeListener, dispatchEvent }) })`. Use `matches: false` so the desktop branch (persistent rail) is exercised — the mobile branch needs a real Sheet open simulation which is more work than it's worth here.
  - React Router's `<NavLink>` adds a literal `"active"` className (in addition to `aria-current="page"`) to the rendered `<a>` when the route matches. Asserting `expect(link).toHaveClass("active")` is the simplest cross-version way to verify active-route highlighting — `aria-current` is also valid and arguably more semantic, but the active class is what shadcn's `data-[active=true]` styles consume via the `<SidebarMenuButton isActive>` prop wiring, so testing the underlying class is fine.
---
