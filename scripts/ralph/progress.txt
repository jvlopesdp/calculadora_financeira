# Ralph Progress Log
Started: Tue Jun  2 17:37:47 PDT 2026
---

## Codebase Patterns
- Static assets live under `public/` (Vite default `publicDir`; there is no explicit `publicDir` in vite.config.ts). Files in `public/` are copied verbatim to `dist/client/` at build. Reference them by absolute path from the SPA (e.g. `/brand/icon.svg`).
- Build is `tsc -b && vite build` via the @cloudflare/vite-plugin; **vite build is SLOW (~6 min)** because it bundles xlsx + fonts. Run builds in background and poll the process, not the output file (output is buffered until the end).
- `bun run typecheck` = `tsc -b --noEmit`; `bun run lint` = `eslint .`; `bun run test` = `vitest run` (foreground only).
- `Env` (`src/server/env.d.ts`) has all-required fields. Adding a new required field breaks every fake-`Env` in `src/server/**/*.test.ts` — grep `BETTER_AUTH_URL:` and add the field next to it in each.
- Server auth/url logic is written as pure exported helpers and unit-tested directly (never instantiate `betterAuth()` — it starts a background D1 init promise that rejects with a fake DB). See `googleSocialProvider`, `buildVerificationUrl`, `buildResetUrl` in `auth.ts`.
- `wrangler` CLI requires Node >= 22; the local sandbox runs Node 20, so `wrangler deploy --dry-run` ACs cannot be executed here — defer to CI.

## 2026-06-02 17:50 - US-001
- Moved brand SVGs into a dedicated stable folder and wired them for build.
- Created `public/brand/`. Moved root `logo-nome.svg` → `public/brand/logo-nome.svg`, `logo-calculadora-financeira.svg` → `public/brand/logo-mark.svg`, `icon-theme.svg` → `public/brand/icon.svg`. Originals removed from repo root by the move.
- No `public/vite.svg` existed (the repo had no `public/` dir at all); `/vite.svg` is only referenced in `index.html` and will be replaced in US-002.
- Files changed: added `public/brand/{logo-nome.svg,logo-mark.svg,icon.svg}`; removed root SVGs.
- Verified: `bun run typecheck` passes; `bun run build` succeeds and emits `dist/client/brand/{logo-nome.svg,logo-mark.svg,icon.svg}`.
- **Learnings for future iterations:**
  - The first build on this branch took ~6m14s — budget time accordingly; subsequent incremental builds may be faster.
  - `logo-nome.svg` is large (~20KB) and is the full wordmark; `logo-mark.svg` (~865B) is the compact mark; `icon.svg` (~950B) is the favicon/touch icon. US-002 should point favicon + apple-touch-icon at `/brand/icon.svg`.
  - US-003 BrandLogo uses `/brand/logo-nome.svg`; BrandMark uses `/brand/logo-mark.svg`.
---

## 2026-06-02 18:45 - US-002
- Updated `index.html` with brand favicon, title, and metadata.
- Changes in `index.html`: favicon `<link rel="icon">` now points to `/brand/icon.svg` (was `/vite.svg`); added `<link rel="apple-touch-icon" href="/brand/icon.svg">`; added `<meta name="theme-color" content="#B94F45">`; added `<meta name="description">` (≤160 chars); `<title>` changed from 'Simulador Financeiro' to 'Calculadora Financeira.app'.
- Files changed: `index.html`.
- Verified: `grep -rn 'vite.svg' src/ index.html public/` returns nothing; `bun run typecheck` passes; `bun run lint` passes (0 errors, only a pre-existing react-refresh warning in site-header.tsx).
- Browser verification: NO browser/dev-browser MCP tooling is available in this environment — manual browser verification of favicon + tab title still needed (NOK for automated check, but functionally trivial for a static index.html change).
- **Learnings for future iterations:**
  - No browser MCP is configured here; stories with "Verify in browser" ACs cannot be auto-verified — note manual verification and rely on typecheck/lint/tests.
  - `index.html` lives at repo root (`/workspace/index.html`), not under `src/` or `public/`. The description meta uses the exact suggested copy. Title is the canonical brand string 'Calculadora Financeira.app' — reuse this exact casing elsewhere (alt-text, og tags) for consistency.
  - Editing `index.html` triggers a harmless "LF will be replaced by CRLF" git warning on this checkout.
---

## 2026-06-02 18:52 - US-003
- Created reusable React brand components `BrandLogo` and `BrandMark`.
- Files changed: added `src/components/brand/brand-logo.tsx`, `src/components/brand/brand-mark.tsx`, `src/components/brand/brand-logo.test.tsx`, `src/components/brand/brand-mark.test.tsx`.
- `BrandLogo` renders `<img src="/brand/logo-nome.svg" alt="Calculadora Financeira.app" loading="eager">`; `BrandMark` uses `/brand/logo-mark.svg`. Both take an optional `className` prop (strict interface, no `any`).
- Verified: `bun run typecheck` passes; `bun run lint` passes (0 errors, only the pre-existing react-refresh warning in site-header.tsx); brand tests pass (4/4) via `bun run test -- src/components/brand`.
- **Learnings for future iterations:**
  - Test pattern: smoke-test `<img>` via `screen.getByAltText("Calculadora Financeira.app")` then assert `toHaveAttribute("src", ...)`. Both logos share the same alt-text, so when both are mounted in the same tree (e.g. US-004 AppShell with responsive show/hide), query by `src` or use `getAllByAltText` to disambiguate.
  - US-004 will wrap these in `<Link>` (BrandLogo → /financiamento in AppShell, → / in AuthLayout) and apply responsive classes (`hidden md:block` / `md:hidden`).
  - Vitest startup here is heavy (~50s for env/collect even for 2 trivial files); scope `bun run test -- <path>` to the story's files when iterating.
---

## 2026-06-02 18:59 - US-004
- Applied BrandLogo/BrandMark to the authenticated AppShell (sidebar header) and the AuthLayout.
- `src/components/app-sidebar.tsx`: replaced the `IconInnerShadowTop` + "Calculadora Financeira" `<span>` placeholder inside the existing `<Link to="/financiamento">` with `<BrandLogo className="hidden h-8 w-auto md:block" />` + `<BrandMark className="h-8 w-auto md:hidden" />` (responsive: wordmark on >=md, compact mark on mobile). Removed the now-unused `IconInnerShadowTop` import. Gave the SidebarMenuButton `h-auto` so the taller logo isn't clipped.
- `src/app/auth-layout.tsx`: removed the old `<header>` + serif "Calculadora Financeira" `<span>`; render a centered `<BrandLogo className="h-10 w-auto" />` wrapped in `<Link to="/">` above the `<Outlet />`.
- Added `src/app/auth-layout.test.tsx`: asserts the BrandLogo renders with `/brand/logo-nome.svg` and its link points to `/`, and that the routed outlet content renders.
- Files changed: `src/components/app-sidebar.tsx`, `src/app/auth-layout.tsx`, `src/app/auth-layout.test.tsx`.
- Verified: `bun run typecheck` passes; `bun run lint` passes (0 errors, only the pre-existing react-refresh warning in site-header.tsx); tests green — app-sidebar (5), site-header (15), brand (4), App routing (8), protected-route (4), auth-layout (2).
- The literal string 'Simulador Financeiro' was already absent from `src/` (the prior placeholder was the brand text + Vite icon "Calculadora Financeira"); AC satisfied. AppShell page-title fallback "Calculadora Financeira" in `site-header.tsx getPageTitle` is a route title (not a logo placeholder) and is intentionally kept.
- Browser verification: NO browser/dev-browser MCP tooling in this environment — manual browser check of the logo in the sidebar/auth pages and the mobile BrandMark swap still needed.
- **Learnings for future iterations:**
  - The "logo placeholder" the PRD calls 'Simulador Financeiro' actually lived as `IconInnerShadowTop` + a "Calculadora Financeira" text span in `app-sidebar.tsx`, and a serif text span in `auth-layout.tsx`. Both are now brand components.
  - `app-sidebar.test.tsx` queries the brand link by `getByRole("link", { name: /calculadora financeira/i })` — the BrandLogo/BrandMark `alt="Calculadora Financeira.app"` satisfies this regex, so the test stayed green without edits. Both imgs share that alt, so within the single sidebar link the accessible name is the alt repeated — still matches.
  - In jsdom the Tailwind `hidden`/`md:block` classes don't apply (no CSS), so both BrandLogo and BrandMark are in the a11y tree during tests; query brand imgs by `src` (or `getAllByAltText`) to disambiguate when both are mounted.
  - AuthLayout had no pre-existing test; added one. It renders under a `<Route element={<AuthLayout/>}>` parent with child routes for the `<Outlet/>`.
---

## 2026-06-02 19:20 - US-005
- Added `PUBLIC_APP_URL` to the typed `Env` and rewrote the verify/reset email links so they land on the existing SPA routes (`/verify-email?token=` and `/reset-password?token=`) instead of Better Auth's `/api/auth/*` endpoint (which falls through to the SPA 404).
- `src/server/env.d.ts`: added required `PUBLIC_APP_URL: string` field to `Env`.
- `wrangler.jsonc`: added `PUBLIC_APP_URL` to `vars` = `https://calculadorafinanceira.app` with a comment documenting the local-dev override (`.dev.vars` / `wrangler dev --var`).
- `.env.example`: documented `PUBLIC_APP_URL=http://localhost:5173` for local dev.
- `src/server/auth.ts`: added pure exported helpers `buildVerificationUrl(publicAppUrl, originalUrl)` and `buildResetUrl(publicAppUrl, originalUrl)` plus a non-throwing `getUrlParam` helper. They extract `token` (and preserve `callbackURL` for verify) via `new URL(...).searchParams`, trim a trailing slash from the base, and encode the token. `sendVerificationEmail` / `sendResetPassword` now pass `env.PUBLIC_APP_URL` through these helpers to the templates.
- `src/server/auth.test.ts` (new): 9 tests covering verify+reset rewrites, callbackURL preservation/omission, trailing-slash trim, non-standard URL token extraction, and malformed-URL empty-token fallback. Follows the `googleSocialProvider` pattern — tests the pure helpers directly, never instantiating `betterAuth()` (which fires a background D1 init promise → unhandled rejections with a fake DB).
- Because `PUBLIC_APP_URL` is a required `Env` field, every server test that builds a fake `Env` needed the field added: `auth-rate-limit-response`, `email`, `health`, `middleware/require-user`, `routes/payments|scenarios|tracker-entries|tracker-plans`, `scheduled`, `turnstile`.
- Verified: `bun run typecheck` passes; `bun run lint` passes (0 errors, only the pre-existing react-refresh warning in site-header.tsx); full server suite green — 134 tests across 13 files (auth.test.ts 9/9).
- **`wrangler deploy --dry-run` AC could NOT be run**: this environment has Node v20.20.2 but wrangler requires >= v22. The config change is a single valid `vars` entry (typecheck/lint cover the rest). NOK only due to the environment Node version — needs to be confirmed in CI where Node 22 is present.
- **Learnings for future iterations:**
  - `Env` (`src/server/env.d.ts`) is a REQUIRED-fields interface. Adding any new required field forces updating EVERY `baseEnv()`/fake-`Env` helper across `src/server/**/*.test.ts` or typecheck fails. Grep for `BETTER_AUTH_URL:` to find them all — they sit next to it.
  - Server auth helpers are deliberately pure + exported so they can be unit-tested WITHOUT calling `betterAuth()` (which starts a background D1 init promise that rejects under a fake DB). Follow this pattern for any new auth logic — see `googleSocialProvider`, `buildVerificationUrl`, `buildResetUrl`.
  - `wrangler` CLI needs Node >= 22; the local sandbox runs Node 20, so `wrangler deploy --dry-run` ACs can't be executed here — note as a CI-only check.
  - US-007 will refactor the email templates and needs `publicAppUrl` threaded into `verifyEmailTemplate`/`resetPasswordTemplate`; the callers in `auth.ts` already build the final action URL via these helpers, so US-007 should pass `env.PUBLIC_APP_URL` as the separate `publicAppUrl` template input (for the logo/footer links), distinct from the action `url`.
---
