# Ralph Progress Log
Started: Wed May 27 19:10:33 PDT 2026
---

## Codebase Patterns
- Vite defines `__COMMIT_SHA__` (see vite.config.ts `define`); the server already declares it in `src/server/env.d.ts`. The SPA-side declaration lives in `src/vite-env.d.ts` so it's visible to `tsconfig.app.json` (which excludes `src/server`).
- Tests live next to source as `*.test.ts(x)` and are picked up by `vitest run` (config in `vite.config.ts`). Always run `bun run test` in foreground only.
- App entrypoints: `src/main.tsx` wires global providers (ThemeProvider, SimulationProvider, then `<App />`); `src/app/App.tsx` owns the router and now the QueryClientProvider. Avoid moving the QueryClientProvider into main.tsx because App.test.tsx renders `<AppRoutes />` directly without it (no providers needed for current tests).
- Path alias `@/*` → `src/*` is configured in both tsconfig.app.json and vite.config.ts.
- `src/tests/setup.ts` only imports `@testing-library/jest-dom/vitest`; jsdom is the global env.
- Server-state hooks live in `src/lib/queries/*` (TanStack Query). queryKey convention: `['scenarios']` (list), `['scenario', id]` (detail), `['scenarios', scenarioId, 'payments']` via `paymentsQueryKey`. Mutations invalidate their list key in `onSuccess`. Plain `fetch` wrappers stay in `src/lib/api-client.ts` and are the queryFn bodies — don't call them directly from components, go through the hooks (or `useQueries` for dynamic lists).
- TEST GOTCHA: any test rendering a component that uses a TanStack hook (`useQuery`/`useMutation`/`useQueryClient`) — including anything under `<AppRoutes/>` or `<AppShell/>` (the shell mounts `MigrateLocalSimulationDialog`) — must wrap the render in `<QueryClientProvider client={new QueryClient({ defaultOptions: { queries: { retry: false } } })}>` or it throws "No QueryClient set".
- Session = `useSession()` from `src/lib/queries/session.ts` (TanStack, queryKey `['session']`). It's the ONLY session source in app code — returns `{ user, session, isPending, isError }`, `null` response → `{ user: null, session: null }`. Sign-out is `useSignOut()` (POST /api/auth/sign-out → invalidates `['session']`). `@/lib/auth-client` (the Better Auth client) is reserved for the auth forms under `src/features/auth/**` (sign-in/up/reset) + the type re-exports in `session.ts`/`api-client.ts`. Don't reintroduce `use-current-user`.
- TEST GOTCHA: when a test mocks `@/lib/queries/session`, the factory must export EVERY function the rendered tree calls. `NavUser` calls both `useSession` AND `useSignOut`; since `AppSidebar`/`AppShell` render `NavUser`, those tests' mocks need `useSignOut: () => ({ mutate: vi.fn() })` too — otherwise "No useSignOut export is defined on the mock". Mocks return the flattened `{ user, session, isPending, isError }` shape, not a raw `UseQueryResult`.
- Better Auth: `createAuth(env)` (in `src/server/auth.ts`) runs fine with a fake `DB: {} as D1Database` (init is lazy w.r.t. queries) and the returned instance exposes the RAW options passed to `betterAuth()` on `.options`. So server tests can assert config (e.g. `createAuth(env).options.socialProviders`) by calling the REAL `createAuth` — no need to mock `betterAuth`. Mocking `better-auth` is risky because `better-auth/api` (`createAuthMiddleware`, used in the before-hook) imports from it.
---

## 2026-05-27 - US-001 — TanStack Query + persister
- Implemented:
  - Added `@tanstack/react-query`, `@tanstack/query-sync-storage-persister`, `@tanstack/react-query-persist-client` to package.json (via `bun add`).
  - `src/lib/query-client.ts` exports a `QueryClient` with defaultOptions { queries: { staleTime: 5min, gcTime: 24h, refetchOnWindowFocus: false } } and a `SESSION_QUERY_KEY` sentinel (`['session']`).
  - `src/lib/query-persister.ts` exports `startQueryPersister()` (idempotent) that wires `persistQueryClient` against `localStorage` with key `cf-app-query-v1`, buster `__COMMIT_SHA__`, and a `shouldDehydrateQuery` that excludes the `['session']` queryKey (to satisfy US-003 ahead of time) and only persists `status === 'success'` queries.
  - `src/app/App.tsx` now wraps `<AppRoutes/>` in `<QueryClientProvider client={queryClient}>` and calls `startQueryPersister()` at module load (side effect, before render).
  - `src/vite-env.d.ts` adds `declare const __COMMIT_SHA__: string;` so the SPA tsconfig sees the Vite-defined global (server-side already had its own declaration).
  - Tests: `src/lib/query-client.test.ts` (default options + session key) and `src/lib/query-persister.test.ts` (asserts persister registered with correct key/buster, session query is excluded, idempotent across calls).
- Files changed:
  - package.json, bun.lockb
  - src/app/App.tsx
  - src/lib/query-client.ts (new)
  - src/lib/query-persister.ts (new)
  - src/lib/query-client.test.ts (new)
  - src/lib/query-persister.test.ts (new)
  - src/vite-env.d.ts
- Quality: `bun run typecheck`, `bun run lint`, `bun run test` all pass (585 tests, 55 files; only pre-existing site-header.tsx warning remains).
- **Learnings for future iterations:**
  - The TanStack `persistQueryClient` API takes a single options object — the `dehydrateOptions.shouldDehydrateQuery` predicate is what controls per-query persistence (NOT a top-level filter).
  - To compare a queryKey against `['session']` in `shouldDehydrateQuery`, compare length + each element — `queryKey.includes('session')` is brittle if the key shape changes.
  - Module top-level side effects (`startQueryPersister()` in App.tsx) are safe here because `App.test.tsx` renders `<AppRoutes />` directly, never importing `App.tsx`. Keep that invariant in future stories that touch App.tsx.
  - When writing a vitest test that uses `vi.resetModules()` + dynamic `import()`, also re-import any sibling module whose identity matters (`queryClient` here), otherwise referential equality checks fail because the top-level import captured the pre-reset instance.
  - For React Query v5, `gcTime` replaces v4's `cacheTime`. Use `gcTime` directly in defaults.
---

## 2026-05-27 - US-002 — api-client refatorado em hooks TanStack Query
- Implemented:
  - `src/lib/queries/scenarios.ts` (novo): `useScenarios({ enabled? })`, `useScenario(id)`, `useCreateScenario()`, `useUpdateScenario()`, `useDeleteScenario()`; exporta `SCENARIOS_QUERY_KEY` (`['scenarios']`) e `scenarioQueryKey(id)` (`['scenario', id]`). Mutations invalidam `['scenarios']` (onSuccess) e semeiam a chave de detalhe via `setQueryData`.
  - `src/lib/queries/payments.ts` (novo): `usePayments(scenarioId)`, `useCreatePayment()`, `useUpdatePayment()`, `useDeletePayment()`; exporta `paymentsQueryKey(scenarioId)` (`['scenarios', scenarioId, 'payments']`). Mutations invalidam a lista de pagamentos do cenário.
  - `src/lib/api-client.ts`: adicionadas funções plain `updateScenario(id, input)` (PATCH) e `deleteScenario(id)` (DELETE, soft-archive) + tipo `UpdateScenarioInput`. As funções plain continuam exportadas para reuso interno dos hooks.
  - Refatorados para usar hooks (sem `useEffect(fetch)` para server-state): `historico-page.tsx` (useScenarios + useQueries p/ pagamentos), `historico-detalhe-page.tsx` (useScenario + usePayments; cache atualizado via setQueryData no save/delete), `new-scenario-dialog.tsx`, `payment-form-dialog.tsx`, `delete-payment-dialog.tsx`, `scenario-combobox.tsx` (lazy via flag `enabled`), `migrate-local-simulation-dialog.tsx`.
  - Tests: `src/lib/queries/scenarios.test.tsx` e `payments.test.tsx` (novos) cobrindo fetch, gating por `enabled`/`id`, e invalidação de queryKeys. Atualizados `scenario-combobox.test.tsx`, `migrate-local-simulation-dialog.test.tsx`, `App.test.tsx`, `financiamento-page.test.tsx` para envolver render em `<QueryClientProvider>`.
- Files changed:
  - src/lib/api-client.ts
  - src/lib/queries/scenarios.ts (new), src/lib/queries/payments.ts (new)
  - src/lib/queries/scenarios.test.tsx (new), src/lib/queries/payments.test.tsx (new)
  - src/features/historico/pages/historico-page.tsx, historico-detalhe-page.tsx
  - src/features/historico/components/{new-scenario-dialog,payment-form-dialog,delete-payment-dialog,scenario-combobox,migrate-local-simulation-dialog}.tsx
  - src/features/historico/components/{scenario-combobox,migrate-local-simulation-dialog}.test.tsx
  - src/app/App.test.tsx
  - src/features/simulator/pages/financiamento-page.test.tsx
- Quality: `bun run typecheck`, `bun run lint` (só o warning pré-existente em site-header.tsx), `bun run test` (595 testes, 57 arquivos) — todos passam.
- **Learnings for future iterations:**
  - O `QueryClientProvider` de produção vive em `src/app/App.tsx` (acima do `BrowserRouter`), NÃO em main.tsx nem em AppRoutes. Qualquer teste que renderize `<AppRoutes/>`, `<AppShell/>` ou um componente do feature `historico` precisa envolver o render num `<QueryClientProvider client={new QueryClient(...)}>`, senão `useQueryClient` lança "No QueryClient set". Use `{ defaultOptions: { queries: { retry: false } } }` nos testes para falhar rápido.
  - Convenção de queryKeys deste app: scenarios → `['scenarios']`; detalhe → `['scenario', id]`; pagamentos de um cenário → `['scenarios', scenarioId, 'payments']` (via helper `paymentsQueryKey`). Mantenha mutations invalidando essas chaves no `onSuccess`.
  - Para listas dinâmicas (N cenários → N consultas de pagamentos) use `useQueries` chamando a função plain `listPayments` no `queryFn` (compartilha o mesmo `paymentsQueryKey` que `usePayments`), em vez de useEffect manual.
  - `useScenarios` aceita `{ enabled }` para preservar o lazy-load do `ScenarioCombobox` (só busca após o primeiro open). Com cache do TanStack, abrir o dropdown várias vezes dispara o fetch só uma vez — o teste "does not refetch on subsequent opens" valida isso sem `vi`.
  - `MigrateLocalSimulationDialog` agora usa `useCreateScenario()`; como ele é montado no `AppShell`, o teste `financiamento-page.test.tsx` (que não mocka api-client) passou a precisar do provider.
---

## 2026-05-27 - US-003 — useSession via TanStack Query como única fonte de verdade da sessão
- Implemented:
  - `src/lib/queries/session.ts` (novo): `useSession()` (queryKey `['session']` via `SESSION_QUERY_KEY`, queryFn `getSession`) devolve `{ user, session, isPending, isError }`, com resposta `null` virando `{ user: null, session: null }`. `useSignOut()` é uma mutation que chama `signOutRequest` (POST /api/auth/sign-out) e invalida `['session']` no `onSuccess`.
  - `src/lib/api-client.ts`: novas funções plain `getSession()` (GET /api/auth/get-session, devolve `SessionResponse | null`) e `signOutRequest()` (POST /api/auth/sign-out); novo tipo `SessionResponse { user: AuthUser; session: AuthSession }` (importa os tipos de `./auth-client`).
  - Removidos `src/lib/use-current-user.ts` e `.test.ts` (a antiga ponte sobre `authClient.useSession`).
  - Migrados de `useCurrentUser` → `useSession`: `src/app/protected-route.tsx` (`isLoading` → `isPending`), `src/components/app-sidebar.tsx`, `src/components/nav-user.tsx` (sign-out agora via `useSignOut().mutate(undefined, { onSuccess })` em vez de `authClient.signOut`), `src/features/historico/components/migrate-local-simulation-dialog.tsx`.
  - Tests: `src/lib/queries/session.test.tsx` (novo) cobre user/session derivados, resposta null → anônimo, isError no reject, e que `useSignOut` posta e invalida `['session']`. Atualizados `App.test.tsx`, `app-sidebar.test.tsx`, `nav-user.test.tsx`, `migrate-local-simulation-dialog.test.tsx` para mockar `@/lib/queries/session`.
  - Persister já excluía `['session']` desde US-001 (`shouldDehydrateQuery` em `query-persister.ts`) — nenhuma mudança necessária.
  - `grep '@/lib/auth-client'` em `src/` agora só aparece em `session.ts` (import de tipos) e nas páginas/forms de auth (`src/features/auth/**`), conforme o critério.
- Files changed:
  - src/lib/queries/session.ts (new), src/lib/queries/session.test.tsx (new)
  - src/lib/api-client.ts
  - src/lib/use-current-user.ts (deleted), src/lib/use-current-user.test.ts (deleted)
  - src/app/protected-route.tsx
  - src/components/app-sidebar.tsx, src/components/nav-user.tsx
  - src/features/historico/components/migrate-local-simulation-dialog.tsx
  - src/app/App.test.tsx, src/components/app-sidebar.test.tsx, src/components/nav-user.test.tsx, src/features/historico/components/migrate-local-simulation-dialog.test.tsx
  - src/components/CLAUDE.md (doc do NavUser/AppSidebar atualizada para useSession/useSignOut)
- Quality: `bun run typecheck` ok, `bun run lint` ok (só o warning pré-existente em site-header.tsx), `bun run test` 595 testes em 57 arquivos — todos passam.
- **Learnings for future iterations:**
  - GOTCHA: ao trocar um hook por outro num componente do shell, os testes que MOCKAM `@/lib/queries/session` precisam exportar TODAS as funções que o componente chama. `NavUser` usa `useSession` E `useSignOut`; `app-sidebar.test.tsx` renderiza `NavUser` indiretamente, então seu mock teve que ganhar `useSignOut: () => ({ mutate: vi.fn() })` além de `useSession` — senão vitest lança "No useSignOut export is defined on the mock".
  - O contrato do `useSession()` é `{ user, session, isPending, isError }` (não `{ data, isLoading }`). Mocks nos testes devem devolver esse shape achatado, não o `UseQueryResult` cru do TanStack.
  - `getSession()` chama `GET /api/auth/get-session` do Better Auth, que responde `200` com `null` quando não há cookie de sessão — por isso o tipo é `SessionResponse | null` e o hook normaliza para anônimo.
---

## 2026-05-27 - US-004 — ProtectedRoute reativo com redirect /login?next=…
- Implemented:
  - `src/app/protected-route.tsx`: o redirect anônimo passou de `<Navigate to="/login" state={{ from }}/>` para `<Navigate to={`/login?next=${encodeURIComponent(location.pathname + location.search)}`} replace />`. O loader `role="status"` "Carregando…" durante `isPending` permanece (já existia desde US-003). `/` continua redirecionando para `/financiamento` (público) — a raiz NÃO está sob `<ProtectedRoute/>` em routes.tsx; só `/historico` e `/historico/:scenarioId` estão.
  - `src/features/auth/pages/login-page.tsx`: `resolveRedirectTarget` agora lê o querystring `?next=` (via `searchParams.get("next")`) em vez de `location.state.from`; mantém a guarda anti-open-redirect (`startsWith("/") && !startsWith("//")`); o **fallback mudou de `/financiamento` para `/historico`** (exigência do AC). Removidos o `useLocation` e o helper `isLocationStateWithFrom` (não usados mais).
  - `src/app/protected-route.test.tsx` (novo): mocka `@/lib/queries/session` (`useSession`), cobre os 3 casos — loading (loader, sem filho), sem-user (Navigate→`/login` com `?next=` carregando pathname+search, ex.: `/historico/42?tab=pagamentos`), com-user (renderiza o filho). Usa um `LoginStub` que lê `useSearchParams().get("next")` num `data-testid="login-next"`.
  - `src/features/auth/pages/login-page.test.tsx`: dois testes atualizados — o caso default agora espera `callbackURL: "/historico"` (novo fallback) e o caso "honours destination" virou "honours the ?next= destination" usando `renderLogin(["/login?next=%2Ffinanciamento"])`. Tipo de `renderLogin` simplificado para `string[]` (a forma `{ pathname, state }` não é mais usada).
- Files changed:
  - src/app/protected-route.tsx
  - src/app/protected-route.test.tsx (new)
  - src/features/auth/pages/login-page.tsx
  - src/features/auth/pages/login-page.test.tsx
- Quality: `bun run typecheck` ok, `bun run lint` ok (só o warning pré-existente em site-header.tsx), `bun run test` 598 testes em 58 arquivos — todos passam.
- Browser: nenhuma ferramenta de browser (MCP/dev-browser) disponível neste ambiente — **verificação manual no browser ainda é necessária** para o critério "Verify in browser".
- **Learnings for future iterations:**
  - O contrato de redirect pós-login deste app agora é `?next=<path+search>` no querystring (NÃO mais `location.state.from`). `ProtectedRoute` produz; `LoginPage` consome via `searchParams.get("next")`. Fallback quando não há `?next=` é `/historico`. Futuras rotas protegidas (ex.: `/acompanhamento/*` na US-015+) ganham esse comportamento de graça só por estarem sob `<ProtectedRoute/>`.
  - Guarda anti-open-redirect: sempre validar `next.startsWith("/") && !next.startsWith("//")` antes de navegar para um destino vindo da URL — bloqueia `//evil.com` e URLs absolutas.
  - `useSearchParams().get("next")` já devolve o valor DECODIFICADO; o `encodeURIComponent` no `ProtectedRoute` e o decode do react-router se cancelam — não decodifique de novo manualmente.
  - O botão "Entrar" do `NavUser` navega para `/login` sem `?next=` (clique manual) — cai no fallback `/historico`, o que é o comportamento desejado.
---

## 2026-05-27 - US-005 — Google como social provider no Better Auth (backend)
- Implemented:
  - `src/server/env.d.ts`: adicionados `GOOGLE_CLIENT_ID?` e `GOOGLE_CLIENT_SECRET?` (strings opcionais) à interface `Env`.
  - `src/server/auth.ts`: dentro do `createAuth`, conditional spread aditivo — `...(env.GOOGLE_CLIENT_ID && env.GOOGLE_CLIENT_SECRET ? { socialProviders: { google: { clientId, clientSecret } } } : {})` — inserido entre `emailAndPassword` e `hooks`. Resto do `createAuth` (rate limit, turnstile, email verification, reset password) intacto. Sem ambos os secrets, nenhuma chave `socialProviders` é emitida.
  - `wrangler.jsonc`: comentário no bloco `vars` listando os secrets (incl. GOOGLE_CLIENT_ID/SECRET) que são definidos via `wrangler secret put`, apontando para docs/google-oauth.md.
  - `docs/google-oauth.md` (novo): passo a passo de credenciais no Google Cloud Console, os 2 redirect URIs (localhost:5173 e calculadorafinanceira.app `/api/auth/callback/google`), comandos `wrangler secret put`, nota sobre VITE_GOOGLE_ENABLED (US-006).
  - `src/server/auth-google.test.ts` (novo): 4 testes chamando o `createAuth` REAL com FakeD1 e inspecionando `.options.socialProviders` — omite quando ID ausente, omite quando só ID, omite quando só secret, e inclui `google: { clientId, clientSecret }` quando ambos presentes.
- Files changed:
  - src/server/env.d.ts
  - src/server/auth.ts
  - wrangler.jsonc
  - docs/google-oauth.md (new)
  - src/server/auth-google.test.ts (new)
- Quality: `bun run typecheck` ok, `bun run lint` ok (só o warning pré-existente em site-header.tsx), `bun run test` 602 testes em 59 arquivos — todos passam.
- **Learnings for future iterations:**
  - Escopo Auth deste branch é ESTRITAMENTE ADITIVO: não refatorar `createAuth`, não trocar adapter D1, não tocar no fluxo email/senha. Gating de social provider via conditional spread mantém o diff localizado.
  - Para testar config do Better Auth, chame o `createAuth` real (roda com `DB: {} as D1Database`) e leia `.options` — o instance expõe as opções cruas passadas a `betterAuth()`. Evite mockar `better-auth` porque `better-auth/api` (createAuthMiddleware no before-hook) depende do módulo real.
  - Better Auth monta o redirect OAuth como `{BETTER_AUTH_URL}/api/auth/callback/google` — os redirect URIs no Google Console têm que bater exatamente (localhost:5173 em dev, calculadorafinanceira.app em prod).
  - US-006 (botão front "Continuar com Google") já está prevista: front esconde o botão até `VITE_GOOGLE_ENABLED='true'`, e o backend só ativa o provider quando ambos os secrets existem — os dois lados ficam OFF por padrão.
---
