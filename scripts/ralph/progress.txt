# Ralph Progress Log
Started: Tue Jun  9 19:30:36 PDT 2026
---

## Codebase Patterns
- Theme tokens live in `src/index.css` (`:root` light + `.dark`) and are exposed as Tailwind utilities via `@theme` (`--color-*`). Use semantic classes (`bg-background`, `bg-primary`, `text-primary-foreground`, `text-foreground`, `text-muted-foreground`) instead of hex colors so light/dark mode stay consistent. `--primary` ≈ the brand red (#B94F45), `--background` ≈ the brand creme (#F7EFE2).
- Quality gate: `bun run typecheck`, `bunx eslint <file>` (or `bun run lint`), `bun run test`. tsc emits a harmless `setlocale` warning — exit code is what matters.
- No browser/MCP tooling available in this environment; UI stories' "Verify in browser" AC must be flagged as needing manual verification.
---

## 2026-06-09 - US-001
- Replaced hardcoded hex colors in the Landing page with theme tokens.
- Files changed: `src/features/landing/pages/landing-page.tsx`
  - `bg-[#F7EFE2]` -> `bg-background` on the root container
  - `bg-[#B94F45] text-white hover:bg-[#B94F45]/90` -> `bg-primary text-primary-foreground hover:bg-primary/90` on the "Criar conta" and "Começar a simular" CTAs
- Quality checks: typecheck passes; eslint on the file passes. No hex colors remain (grep clean).
- **Learnings for future iterations:**
  - Theme tokens are already defined for both light and dark in `src/index.css`; the brand creme/red map cleanly to `--background`/`--primary`, so no new CSS was needed.
  - The default shadcn `Button` variant is already `bg-primary`; the explicit classes here are kept to satisfy the AC wording but are effectively redundant for the primary CTAs.
  - Browser verification could not be performed (no dev-browser/MCP tools in this environment) — manual browser check of light/dark still recommended for US-002/US-003.
---
